(* Generated by F* Custard extraction. Do not edit. *)
[@@@ocaml.warning "-3-5-8-11-20-26-27-28-32-33-34-35-37-39-50-57-60-69-70"]

type ident = {
  idText : string;
  idRange : FStarC_Range_Type.range;
}

type lident = {
  ns : (ident) list;
  ident : ident;
  nsstr : string;
  str : string;
}

type ipath = (ident) list

type lid = lident

let set_id_range (r : FStarC_Range_Type.range) (i : ident) : ident =
  { idText = (i).idText; idRange = r }

let set_lid_range (l : lident) (r : FStarC_Range_Type.range) : lident =
  { ns = (l).ns;
    ident = (let tmp = (l).ident in
    { idText = (tmp).idText; idRange = r });
    nsstr = (l).nsstr;
    str = (l).str }

let range_of_id (id : ident) : FStarC_Range_Type.range =
  (id).idRange

let ident_equals (id1 : ident) (id2 : ident) : bool =
  ((=) (id1).idText (id2).idText)

let range_of_lid (lid : lident) : FStarC_Range_Type.range =
  (range_of_id (lid).ident)

let lid_equals (l1 : lident) (l2 : lident) : bool =
  ((=) (l1).str (l2).str)

type path = (string) list

let mk_ident (tmp : (string * FStarC_Range_Type.range)) : ident =
  (match tmp with
    | (text, range) -> { idText = text; idRange = range }
  )

let string_of_id (id : ident) : string =
  (id).idText

let text_of_path (path : (string) list) : string =
  (FStarC_Util.concat_l "." path)

let lid_of_ns_and_id (ns : (ident) list) (id : ident) : lident =
  (let tmp = (FStarC_List.map string_of_id ns) in
  let nsstr_ = (text_of_path tmp) in
  { ns = ns;
    ident = id;
    nsstr = nsstr_;
    str = (if ((=) nsstr_ "") then (id).idText else (Prims.strcat nsstr_ (Prims.strcat "." (id).idText))) })

let lid_of_ids (ids : (ident) list) : lident =
  (let tmp = (FStarC_Util.prefix ids) in
  (match tmp with
    | (ns, id) -> (lid_of_ns_and_id ns id)
  ))

let lid_of_path (path : (string) list) (pos : FStarC_Range_Type.range) : lident =
  (let ids = (FStarC_List.map (fun s -> (mk_ident (s, pos))) path) in
  (lid_of_ids ids))

let reserved_prefix : string =
  "uu___"

let string_of_lid (lid : lident) : string =
  (lid).str

let ident_of_lid (l : lident) : ident =
  (l).ident

let fStarC_Class_Show_show__ident (tmp : ident) : string =
  (string_of_id tmp)

let path_of_lid (lid : lident) : (string) list =
  (FStarC_List.map string_of_id (FStarC_List.op_At (lid).ns ((lid).ident :: [])))

let path_of_ns (ns : (ident) list) : (string) list =
  (FStarC_List.map string_of_id ns)

let path_of_text (text : string) : (string) list =
  (FStarC_String.split (46 :: []) text)

let nsstr (l : lident) : string =
  (l).nsstr

let ns_of_lid (lid : lident) : (ident) list =
  (lid).ns

let id_of_text (str : string) : ident =
  (mk_ident (str, FStarC_Range_Type.dummyRange))

let lid_of_str (str : string) : lident =
  (let tmp = (FStarC_List.map id_of_text (FStarC_Util.split str ".")) in
  (lid_of_ids tmp))

let fStarC_Class_Show_show__lident (tmp : lident) : string =
  (string_of_lid tmp)

let rec fStarC_Class_Show_show__show_list_aux__list_lident (l : (lident) list) : string =
  (match l with
    | [] -> ""
    | (x :: []) -> (fStarC_Class_Show_show__lident x)
    | (x :: xs) -> (let tmp = (fStarC_Class_Show_show__lident x) in
      let tmp1 = (fStarC_Class_Show_show__show_list_aux__list_lident xs) in
      let tmp2 = (Prims.strcat ", " tmp1) in
      (Prims.strcat tmp tmp2))
  )

let fStarC_Class_Show_show__list_lident (tmp : (lident) list) : string =
  (let tmp1 = (fStarC_Class_Show_show__show_list_aux__list_lident tmp) in
  let tmp2 = (Prims.strcat tmp1 "]") in
  (Prims.strcat "[" tmp2))

let fStarC_Class_Show_show__option_list_lident (tmp : ((lident) list) option) : string =
  (match tmp with
    | None -> "None"
    | (Some (x)) -> (let tmp1 = (fStarC_Class_Show_show__list_lident x) in
      (Prims.strcat "Some " tmp1))
  )

let qual_id (lid : lident) (id : ident) : lident =
  (let tmp = (lid_of_ids (FStarC_List.op_At (lid).ns ((lid).ident :: (id :: [])))) in
  (set_lid_range tmp (range_of_id id)))

let fStarC_Class_Show_show__option_ident (tmp : (ident) option) : string =
  (match tmp with
    | None -> "None"
    | (Some (x)) -> (let tmp1 = (fStarC_Class_Show_show__ident x) in
      (Prims.strcat "Some " tmp1))
  )

let fStarC_Class_Show_show__tuple2_ident_option_ident (tmp : (ident * (ident) option)) : string =
  (match tmp with
    | (x1, x2) -> (let tmp1 = (fStarC_Class_Show_show__ident x1) in
      let tmp2 = (fStarC_Class_Show_show__option_ident x2) in
      let tmp3 = (Prims.strcat tmp2 ")") in
      let tmp4 = (Prims.strcat ", " tmp3) in
      let tmp5 = (Prims.strcat tmp1 tmp4) in
      (Prims.strcat "(" tmp5))
  )

let rec fStarC_Class_Show_show__show_list_aux__list_tuple2_ident_option (l : ((ident * (ident) option)) list) : string =
  (match l with
    | [] -> ""
    | (x :: []) -> (fStarC_Class_Show_show__tuple2_ident_option_ident x)
    | (x :: xs) -> (let tmp = (fStarC_Class_Show_show__tuple2_ident_option_ident x) in
      let tmp1 = (fStarC_Class_Show_show__show_list_aux__list_tuple2_ident_option xs) in
      let tmp2 = (Prims.strcat ", " tmp1) in
      (Prims.strcat tmp tmp2))
  )

let fStarC_Class_Show_show__list_tuple2_ident_option (tmp : ((ident * (ident) option)) list) : string =
  (let tmp1 = (fStarC_Class_Show_show__show_list_aux__list_tuple2_ident_option tmp) in
  let tmp2 = (Prims.strcat tmp1 "]") in
  (Prims.strcat "[" tmp2))

let ids_of_lid (lid : lident) : (ident) list =
  (FStarC_List.op_At (lid).ns ((lid).ident :: []))

let fStarC_Class_PP_pp__lident (tmp : lident) : FStar_Pprint.document =
  (let tmp1 = (fStarC_Class_Show_show__lident tmp) in
  (FStar_Pprint.arbitrary_string tmp1))

let fStarC_Class_HasRange_pos__ident (tmp : ident) : FStarC_Range_Type.range =
  (range_of_id tmp)

let fStarC_Class_HasRange_pos__lident (tmp : lident) : FStarC_Range_Type.range =
  (fStarC_Class_HasRange_pos__ident (tmp).ident)

let gen' (s : string) (r : FStarC_Range_Type.range) : ident =
  (let i = (FStarC_GenSym.next_id ()) in
  let tmp = (FStarC_Class_Show.fStarC_Class_Show_show__int i) in
  let tmp1 = (Prims.strcat s tmp) in
  let tmp2 = (tmp1, r) in
  (mk_ident tmp2))

let gen (r : FStarC_Range_Type.range) : ident =
  (gen' reserved_prefix r)

let rec fStarC_Class_Show_show__show_list_aux__list_ident (l : (ident) list) : string =
  (match l with
    | [] -> ""
    | (x :: []) -> (fStarC_Class_Show_show__ident x)
    | (x :: xs) -> (let tmp = (fStarC_Class_Show_show__ident x) in
      let tmp1 = (fStarC_Class_Show_show__show_list_aux__list_ident xs) in
      let tmp2 = (Prims.strcat ", " tmp1) in
      (Prims.strcat tmp tmp2))
  )

let fStarC_Class_Show_show__list_ident (tmp : (ident) list) : string =
  (let tmp1 = (fStarC_Class_Show_show__show_list_aux__list_ident tmp) in
  let tmp2 = (Prims.strcat tmp1 "]") in
  (Prims.strcat "[" tmp2))

let fStarC_Class_PP_pp__list_lident (tmp : (lident) list) : FStar_Pprint.document =
  (let tmp1 = (FStarC_Pprint.flow_map (FStar_Pprint.op_Hat_Hat FStar_Pprint.semi (FStar_Pprint.break_ (Prims.parse_int "1"))) fStarC_Class_PP_pp__lident tmp) in
  (FStarC_Class_PP.gbrackets tmp1))

let fStarC_Class_PP_pp__ident (tmp : ident) : FStar_Pprint.document =
  (let tmp1 = (fStarC_Class_Show_show__ident tmp) in
  (FStar_Pprint.arbitrary_string tmp1))

let fStarC_Class_Show_show__option_lident (tmp : (lident) option) : string =
  (match tmp with
    | None -> "None"
    | (Some (x)) -> (let tmp1 = (fStarC_Class_Show_show__lident x) in
      (Prims.strcat "Some " tmp1))
  )

let rec fStarC_Class_Show_show__show_list_aux__list_lident_87 (l : (lident) list) : string =
  (match l with
    | [] -> ""
    | (x :: []) -> (fStarC_Class_Show_show__lident x)
    | (x :: xs) -> (let tmp = (fStarC_Class_Show_show__lident x) in
      let tmp1 = (fStarC_Class_Show_show__show_list_aux__list_lident_87 xs) in
      let tmp2 = (Prims.strcat ", " tmp1) in
      (Prims.strcat tmp tmp2))
  )

let fStarC_Class_Show_show__list_lident_87 (tmp : (lident) list) : string =
  (let tmp1 = (fStarC_Class_Show_show__show_list_aux__list_lident_87 tmp) in
  let tmp2 = (Prims.strcat tmp1 "]") in
  (Prims.strcat "[" tmp2))

let rec fStarC_Class_Show_show__show_list_aux__list_ident_109 (l : (ident) list) : string =
  (match l with
    | [] -> ""
    | (x :: []) -> (fStarC_Class_Show_show__ident x)
    | (x :: xs) -> (let tmp = (fStarC_Class_Show_show__ident x) in
      let tmp1 = (fStarC_Class_Show_show__show_list_aux__list_ident_109 xs) in
      let tmp2 = (Prims.strcat ", " tmp1) in
      (Prims.strcat tmp tmp2))
  )

let fStarC_Class_Show_show__list_ident_109 (tmp : (ident) list) : string =
  (let tmp1 = (fStarC_Class_Show_show__show_list_aux__list_ident_109 tmp) in
  let tmp2 = (Prims.strcat tmp1 "]") in
  (Prims.strcat "[" tmp2))

let fStarC_Class_Show_show__option_list_ident (tmp : ((ident) list) option) : string =
  (match tmp with
    | None -> "None"
    | (Some (x)) -> (let tmp1 = (fStarC_Class_Show_show__list_ident_109 x) in
      (Prims.strcat "Some " tmp1))
  )

let id_as_lid (id : ident) : lident =
  (lid_of_ns_and_id [] id)

let lid_add_suffix (l : lident) (s : string) : lident =
  (let path = (path_of_lid l) in
  (lid_of_path (FStarC_List.op_At path (s :: [])) (range_of_lid l)))

let fStarC_Class_HasRange_setPos__ident (tmp : FStarC_Range_Type.range) (tmp1 : ident) : ident =
  { idText = (tmp1).idText; idRange = tmp }

let fStarC_Class_HasRange_setPos__lident (tmp : FStarC_Range_Type.range) (tmp1 : lident) : lident =
  (let tmp2 = (fStarC_Class_HasRange_setPos__ident tmp (tmp1).ident) in
  { ns = (tmp1).ns; ident = tmp2; nsstr = (tmp1).nsstr; str = (tmp1).str })

let fStarC_Class_Ord_cmp__lident_12 (tmp : lident) (tmp1 : lident) : FStarC_Order.order =
  (FStarC_Class_Ord.fStarC_Class_Ord_cmp__string (string_of_lid tmp) (string_of_lid tmp1))

let fStarC_Class_Ord_cmp__ident_14 (tmp : ident) (tmp1 : ident) : FStarC_Order.order =
  (FStarC_Class_Ord.fStarC_Class_Ord_cmp__string (string_of_id tmp) (string_of_id tmp1))

let fStarC_Class_Ord_cmp__tuple2_lident_ident (tmp : (lident * ident)) (tmp1 : (lident * ident)) : FStarC_Order.order =
  (match (tmp, tmp1) with
    | ((x1, x2), (y1, y2)) -> (let tmp2 = (fStarC_Class_Ord_cmp__lident_12 x1 y1) in
      (FStarC_Order.lex tmp2 (fun tmp3 -> (fStarC_Class_Ord_cmp__ident_14 x2 y2))))
  )

let fStarC_Class_Ord_cmp__list_ident (tmp : (ident) list) (tmp1 : (ident) list) : FStarC_Order.order =
  (FStarC_Order.compare_list tmp tmp1 fStarC_Class_Ord_cmp__ident_14)

let fStarC_Class_Ord_cmp__tuple2_list_ident_list_ident (tmp : ((ident) list * (ident) list)) (tmp1 : ((ident) list * (ident) list)) : FStarC_Order.order =
  (match (tmp, tmp1) with
    | ((x1, x2), (y1, y2)) -> (let tmp2 = (fStarC_Class_Ord_cmp__list_ident x1 y1) in
      (FStarC_Order.lex tmp2 (fun tmp3 -> (fStarC_Class_Ord_cmp__list_ident x2 y2))))
  )

let fStarC_Class_Show_show__tuple2_list_string_lident (tmp : ((string) list * lident)) : string =
  (match tmp with
    | (x1, x2) -> (let tmp1 = (FStarC_Class_Show.fStarC_Class_Show_show__list_string x1) in
      let tmp2 = (fStarC_Class_Show_show__lident x2) in
      let tmp3 = (Prims.strcat tmp2 ")") in
      let tmp4 = (Prims.strcat ", " tmp3) in
      let tmp5 = (Prims.strcat tmp1 tmp4) in
      (Prims.strcat "(" tmp5))
  )

let fStarC_Class_Show_show__t_tuple2_list_lident (tmp : (((string) list * lident)) FStarC_SMap.t) : string =
  (FStarC_SMap.fold tmp (fun k v acc -> (let tmp1 = (FStarC_Class_Show.fStarC_Class_Show_show__string k) in
  let tmp2 = (fStarC_Class_Show_show__tuple2_list_string_lident v) in
  (FStarC_Format.fmt3 "%s -> %s\n%s" tmp1 tmp2 acc))) "")

