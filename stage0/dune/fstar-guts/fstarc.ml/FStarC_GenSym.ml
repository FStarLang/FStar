(* Generated by F* Custard extraction. Do not edit. *)
[@@@ocaml.warning "-3-5-8-11-20-26-27-28-32-33-34-35-37-39-50-57-60-69-70"]

let gensym_st : (Prims.int ref) =
  (ref (Prims.parse_int "0"))

let with_frozen_gensym (f : (unit -> 'u_'a)) : 'u_'a =
  (let v = (!(gensym_st)) in
  let r = (try (f ()) with
      | u__cexn -> (((gensym_st) := v);
        (raise u__cexn))
    ) in
  ((gensym_st) := v);
  r)

let next_id (tmp : unit) : Prims.int =
  (let r = (!(gensym_st)) in
  ((gensym_st) := (Prims.op_Plus r (Prims.parse_int "1")));
  r)

let reset_gensym (tmp : unit) : unit =
  ((gensym_st) := (Prims.parse_int "0"))

