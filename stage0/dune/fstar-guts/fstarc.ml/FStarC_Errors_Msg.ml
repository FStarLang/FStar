(* Generated by F* Custard extraction. Do not edit. *)
[@@@ocaml.warning "-3-5-8-11-20-26-27-28-32-33-34-35-37-39-50-57-60-69-70"]

let subdoc' (indent : bool) (d : FStar_Pprint.document) : FStar_Pprint.document =
  (if ((=) d FStar_Pprint.empty) then FStar_Pprint.empty else (FStar_Pprint.op_Hat_Hat (if indent then (FStar_Pprint.blank (Prims.parse_int "2")) else FStar_Pprint.empty) (FStar_Pprint.op_Hat_Hat (FStar_Pprint.doc_of_string "-") (FStar_Pprint.op_Hat_Hat (FStar_Pprint.blank (Prims.parse_int "1")) (FStar_Pprint.op_Hat_Hat (FStar_Pprint.align d) FStar_Pprint.hardline)))))

let subdoc (d : FStar_Pprint.document) : FStar_Pprint.document =
  (subdoc' true d)

let render_as_doc (ds : (FStar_Pprint.document) list) : FStar_Pprint.document =
  (let tmp = (FStarC_List.map (fun d -> (subdoc (FStar_Pprint.group d))) ds) in
  (FStar_Pprint.concat tmp))

let renderdoc (d : FStar_Pprint.document) : string =
  (let one = (FStarC_Util.float_of_string "1.0") in
  (FStarC_Pprint.pretty_string one (Prims.parse_int "80") d))

let rendermsg (ds : (FStar_Pprint.document) list) : string =
  (let tmp = (render_as_doc ds) in
  (renderdoc tmp))

let text (s : string) : FStar_Pprint.document =
  (FStar_Pprint.flow (FStar_Pprint.break_ (Prims.parse_int "1")) (FStar_Pprint.words s))

let backtrace_doc (tmp : unit) : FStar_Pprint.document =
  (let s = (FStarC_Util.stack_dump ()) in
  (FStar_Pprint.op_Hat_Slash_Hat (text "Stack trace:") (FStar_Pprint.arbitrary_string (FStarC_Util.trim_string s))))

let fStarC_Errors_Msg_to_doc_list__string (tmp : string) : (FStar_Pprint.document) list =
  ((FStar_Pprint.arbitrary_string tmp) :: [])

let json_of_error_message (err_msg : (FStar_Pprint.document) list) : FStarC_Json.json =
  (let tmp = (FStarC_List.map (fun doc -> (let tmp = (renderdoc doc) in
    (FStarC_Json.JsonStr (tmp)))) err_msg) in
  (FStarC_Json.JsonList (tmp)))

let mkmsg (s : string) : (FStar_Pprint.document) list =
  ((FStar_Pprint.arbitrary_string s) :: [])

let vconcat (ds : (FStar_Pprint.document) list) : FStar_Pprint.document =
  (match ds with
    | (h :: t) -> (FStarC_List.fold_left (fun l r -> (FStar_Pprint.op_Hat_Hat l (FStar_Pprint.op_Hat_Hat FStar_Pprint.hardline r))) h t)
    | [] -> FStar_Pprint.empty
  )

let sublist (bullet : FStar_Pprint.document) (elems : (FStar_Pprint.document) list) : FStar_Pprint.document =
  (let tmp = (FStarC_List.map (fun d -> (FStar_Pprint.op_Hat_Hat bullet d)) elems) in
  let tmp1 = (vconcat tmp) in
  let tmp2 = (FStar_Pprint.align tmp1) in
  let tmp3 = (FStar_Pprint.op_Hat_Hat FStar_Pprint.hardline tmp2) in
  (FStar_Pprint.nest (Prims.parse_int "2") tmp3))

let bulleted (ds : (FStar_Pprint.document) list) : FStar_Pprint.document =
  (sublist (FStar_Pprint.doc_of_string "- ") ds)

let fquotes (d : FStar_Pprint.document) : FStar_Pprint.document =
  (FStar_Pprint.enclose (FStar_Pprint.utf8string "‘") (FStar_Pprint.utf8string "’") d)

