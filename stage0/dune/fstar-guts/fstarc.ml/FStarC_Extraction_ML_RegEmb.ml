(* Generated by F* Custard extraction. Do not edit. *)
[@@@ocaml.warning "-3-5-8-11-20-26-27-28-32-33-34-35-37-39-50-57-60-69-70"]

exception NoEmbedding of string

type embedding_kind =
  | SyntaxTerm
  | NBETerm


let mk (e : FStarC_Extraction_ML_Syntax.mlexpr') : FStarC_Extraction_ML_Syntax.mlexpr =
  (FStarC_Extraction_ML_Syntax.with_ty FStarC_Extraction_ML_Syntax.MLTY_Top e)

let mk_binder (x : string) (t : FStarC_Extraction_ML_Syntax.mlty) : FStarC_Extraction_ML_Syntax.mlbinder =
  { FStarC_Extraction_ML_Syntax.mlbinder_name = x;
    mlbinder_ty = t;
    mlbinder_attrs = [] }

let ml_lam (nm : string) (e : FStarC_Extraction_ML_Syntax.mlexpr) : FStarC_Extraction_ML_Syntax.mlexpr =
  (mk (FStarC_Extraction_ML_Syntax.MLE_Fun (((mk_binder nm FStarC_Extraction_ML_Syntax.MLTY_Top) :: []), e)))

let as_name (mlp : ((string) list * string)) : FStarC_Extraction_ML_Syntax.mlexpr =
  (FStarC_Extraction_ML_Syntax.with_ty FStarC_Extraction_ML_Syntax.MLTY_Top (FStarC_Extraction_ML_Syntax.MLE_Name (mlp)))

type embedding_data = {
  arity : Prims.int;
  syn_emb : FStarC_Ident.lident;
  nbe_emb : (FStarC_Ident.lident) option;
}

let local_fv_embeddings : (((FStarC_Ident.lident * embedding_data)) list ref) =
  (ref [])

let builtin_embeddings : ((FStarC_Ident.lident * embedding_data)) list =
  (let syn_emb_lid = (fun s -> (FStarC_Ident.lid_of_path ("FStarC" :: ("Syntax" :: ("Embeddings" :: (s :: [])))) FStarC_Range_Type.dummyRange)) in
  let nbe_emb_lid = (fun s -> (FStarC_Ident.lid_of_path ("FStarC" :: ("TypeChecker" :: ("NBETerm" :: (s :: [])))) FStarC_Range_Type.dummyRange)) in
  let refl_emb_lid = (fun s -> (FStarC_Ident.lid_of_path ("FStarC" :: ("Reflection" :: ("V2" :: ("Embeddings" :: (s :: []))))) FStarC_Range_Type.dummyRange)) in
  let nbe_refl_emb_lid = (fun s -> (FStarC_Ident.lid_of_path ("FStarC" :: ("Reflection" :: ("V2" :: ("NBEEmbeddings" :: (s :: []))))) FStarC_Range_Type.dummyRange)) in
  let tmp = (syn_emb_lid "e_int") in
  let tmp1 = (nbe_emb_lid "e_int") in
  let tmp2 = (Some (tmp1)) in
  let tmp3 = { arity = (Prims.parse_int "0"); syn_emb = tmp; nbe_emb = tmp2 } in
  let tmp4 = (FStarC_Parser_Const.int_lid, tmp3) in
  let tmp5 = (syn_emb_lid "e_bool") in
  let tmp6 = (nbe_emb_lid "e_bool") in
  let tmp7 = (Some (tmp6)) in
  let tmp8 = { arity = (Prims.parse_int "0"); syn_emb = tmp5; nbe_emb = tmp7 } in
  let tmp9 = (FStarC_Parser_Const.bool_lid, tmp8) in
  let tmp10 = (syn_emb_lid "e_unit") in
  let tmp11 = (nbe_emb_lid "e_unit") in
  let tmp12 = (Some (tmp11)) in
  let tmp13 = { arity = (Prims.parse_int "0"); syn_emb = tmp10; nbe_emb = tmp12 } in
  let tmp14 = (FStarC_Parser_Const.unit_lid, tmp13) in
  let tmp15 = (syn_emb_lid "e_string") in
  let tmp16 = (nbe_emb_lid "e_string") in
  let tmp17 = (Some (tmp16)) in
  let tmp18 = { arity = (Prims.parse_int "0"); syn_emb = tmp15; nbe_emb = tmp17 } in
  let tmp19 = (FStarC_Parser_Const.string_lid, tmp18) in
  let tmp20 = (syn_emb_lid "e_norm_step") in
  let tmp21 = (nbe_emb_lid "e_norm_step") in
  let tmp22 = (Some (tmp21)) in
  let tmp23 = { arity = (Prims.parse_int "0"); syn_emb = tmp20; nbe_emb = tmp22 } in
  let tmp24 = (FStarC_Parser_Const.norm_step_lid, tmp23) in
  let tmp25 = (syn_emb_lid "e_range") in
  let tmp26 = (nbe_emb_lid "e_range") in
  let tmp27 = (Some (tmp26)) in
  let tmp28 = { arity = (Prims.parse_int "0"); syn_emb = tmp25; nbe_emb = tmp27 } in
  let tmp29 = (FStarC_Parser_Const.range_lid, tmp28) in
  let tmp30 = (syn_emb_lid "e_vconfig") in
  let tmp31 = (nbe_emb_lid "e_vconfig") in
  let tmp32 = (Some (tmp31)) in
  let tmp33 = { arity = (Prims.parse_int "0"); syn_emb = tmp30; nbe_emb = tmp32 } in
  let tmp34 = (FStarC_Parser_Const.vconfig_lid, tmp33) in
  let tmp35 = (syn_emb_lid "e_list") in
  let tmp36 = (nbe_emb_lid "e_list") in
  let tmp37 = (Some (tmp36)) in
  let tmp38 = { arity = (Prims.parse_int "1"); syn_emb = tmp35; nbe_emb = tmp37 } in
  let tmp39 = (FStarC_Parser_Const.list_lid, tmp38) in
  let tmp40 = (syn_emb_lid "e_option") in
  let tmp41 = (nbe_emb_lid "e_option") in
  let tmp42 = (Some (tmp41)) in
  let tmp43 = { arity = (Prims.parse_int "1"); syn_emb = tmp40; nbe_emb = tmp42 } in
  let tmp44 = (FStarC_Parser_Const.option_lid, tmp43) in
  let tmp45 = (syn_emb_lid "e_sealed") in
  let tmp46 = (nbe_emb_lid "e_sealed") in
  let tmp47 = (Some (tmp46)) in
  let tmp48 = { arity = (Prims.parse_int "1"); syn_emb = tmp45; nbe_emb = tmp47 } in
  let tmp49 = (FStarC_Parser_Const.sealed_lid, tmp48) in
  let tmp50 = (FStarC_Parser_Const_Tuples.mk_tuple_lid (Prims.parse_int "2") FStarC_Range_Type.dummyRange) in
  let tmp51 = (syn_emb_lid "e_tuple2") in
  let tmp52 = (nbe_emb_lid "e_tuple2") in
  let tmp53 = (Some (tmp52)) in
  let tmp54 = { arity = (Prims.parse_int "2"); syn_emb = tmp51; nbe_emb = tmp53 } in
  let tmp55 = (tmp50, tmp54) in
  let tmp56 = (FStarC_Parser_Const_Tuples.mk_tuple_lid (Prims.parse_int "3") FStarC_Range_Type.dummyRange) in
  let tmp57 = (syn_emb_lid "e_tuple3") in
  let tmp58 = (nbe_emb_lid "e_tuple3") in
  let tmp59 = (Some (tmp58)) in
  let tmp60 = { arity = (Prims.parse_int "3"); syn_emb = tmp57; nbe_emb = tmp59 } in
  let tmp61 = (tmp56, tmp60) in
  let tmp62 = (syn_emb_lid "e_either") in
  let tmp63 = (nbe_emb_lid "e_either") in
  let tmp64 = (Some (tmp63)) in
  let tmp65 = { arity = (Prims.parse_int "2"); syn_emb = tmp62; nbe_emb = tmp64 } in
  let tmp66 = (FStarC_Parser_Const.either_lid, tmp65) in
  let tmp67 = (FStarC_Reflection_V2_Constants.fstar_refl_types_lid "namedv") in
  let tmp68 = (refl_emb_lid "e_namedv") in
  let tmp69 = (nbe_refl_emb_lid "e_namedv") in
  let tmp70 = (Some (tmp69)) in
  let tmp71 = { arity = (Prims.parse_int "0"); syn_emb = tmp68; nbe_emb = tmp70 } in
  let tmp72 = (tmp67, tmp71) in
  let tmp73 = (FStarC_Reflection_V2_Constants.fstar_refl_types_lid "bv") in
  let tmp74 = (refl_emb_lid "e_bv") in
  let tmp75 = (nbe_refl_emb_lid "e_bv") in
  let tmp76 = (Some (tmp75)) in
  let tmp77 = { arity = (Prims.parse_int "0"); syn_emb = tmp74; nbe_emb = tmp76 } in
  let tmp78 = (tmp73, tmp77) in
  let tmp79 = (FStarC_Reflection_V2_Constants.fstar_refl_types_lid "binder") in
  let tmp80 = (refl_emb_lid "e_binder") in
  let tmp81 = (nbe_refl_emb_lid "e_binder") in
  let tmp82 = (Some (tmp81)) in
  let tmp83 = { arity = (Prims.parse_int "0"); syn_emb = tmp80; nbe_emb = tmp82 } in
  let tmp84 = (tmp79, tmp83) in
  let tmp85 = (FStarC_Reflection_V2_Constants.fstar_refl_types_lid "term") in
  let tmp86 = (refl_emb_lid "e_term") in
  let tmp87 = (nbe_refl_emb_lid "e_term") in
  let tmp88 = (Some (tmp87)) in
  let tmp89 = { arity = (Prims.parse_int "0"); syn_emb = tmp86; nbe_emb = tmp88 } in
  let tmp90 = (tmp85, tmp89) in
  let tmp91 = (FStarC_Reflection_V2_Constants.fstar_refl_types_lid "env") in
  let tmp92 = (refl_emb_lid "e_env") in
  let tmp93 = (nbe_refl_emb_lid "e_env") in
  let tmp94 = (Some (tmp93)) in
  let tmp95 = { arity = (Prims.parse_int "0"); syn_emb = tmp92; nbe_emb = tmp94 } in
  let tmp96 = (tmp91, tmp95) in
  let tmp97 = (FStarC_Reflection_V2_Constants.fstar_refl_types_lid "fv") in
  let tmp98 = (refl_emb_lid "e_fv") in
  let tmp99 = (nbe_refl_emb_lid "e_fv") in
  let tmp100 = (Some (tmp99)) in
  let tmp101 = { arity = (Prims.parse_int "0"); syn_emb = tmp98; nbe_emb = tmp100 } in
  let tmp102 = (tmp97, tmp101) in
  let tmp103 = (FStarC_Reflection_V2_Constants.fstar_refl_types_lid "comp") in
  let tmp104 = (refl_emb_lid "e_comp") in
  let tmp105 = (nbe_refl_emb_lid "e_comp") in
  let tmp106 = (Some (tmp105)) in
  let tmp107 = { arity = (Prims.parse_int "0"); syn_emb = tmp104; nbe_emb = tmp106 } in
  let tmp108 = (tmp103, tmp107) in
  let tmp109 = (FStarC_Reflection_V2_Constants.fstar_refl_types_lid "sigelt") in
  let tmp110 = (refl_emb_lid "e_sigelt") in
  let tmp111 = (nbe_refl_emb_lid "e_sigelt") in
  let tmp112 = (Some (tmp111)) in
  let tmp113 = { arity = (Prims.parse_int "0"); syn_emb = tmp110; nbe_emb = tmp112 } in
  let tmp114 = (tmp109, tmp113) in
  let tmp115 = (FStarC_Reflection_V2_Constants.fstar_refl_types_lid "ctx_uvar_and_subst") in
  let tmp116 = (refl_emb_lid "e_ctx_uvar_and_subst") in
  let tmp117 = (nbe_refl_emb_lid "e_ctx_uvar_and_subst") in
  let tmp118 = (Some (tmp117)) in
  let tmp119 = { arity = (Prims.parse_int "0"); syn_emb = tmp116; nbe_emb = tmp118 } in
  let tmp120 = (tmp115, tmp119) in
  let tmp121 = (FStarC_Reflection_V2_Constants.fstar_refl_types_lid "letbinding") in
  let tmp122 = (refl_emb_lid "e_letbinding") in
  let tmp123 = (nbe_refl_emb_lid "e_letbinding") in
  let tmp124 = (Some (tmp123)) in
  let tmp125 = { arity = (Prims.parse_int "0"); syn_emb = tmp122; nbe_emb = tmp124 } in
  let tmp126 = (tmp121, tmp125) in
  let tmp127 = (FStarC_Reflection_V2_Constants.fstar_refl_types_lid "ident") in
  let tmp128 = (refl_emb_lid "e_ident") in
  let tmp129 = (nbe_refl_emb_lid "e_ident") in
  let tmp130 = (Some (tmp129)) in
  let tmp131 = { arity = (Prims.parse_int "0"); syn_emb = tmp128; nbe_emb = tmp130 } in
  let tmp132 = (tmp127, tmp131) in
  let tmp133 = (FStarC_Reflection_V2_Constants.fstar_refl_types_lid "universe_uvar") in
  let tmp134 = (refl_emb_lid "e_universe_uvar") in
  let tmp135 = (nbe_refl_emb_lid "e_universe_uvar") in
  let tmp136 = (Some (tmp135)) in
  let tmp137 = { arity = (Prims.parse_int "0"); syn_emb = tmp134; nbe_emb = tmp136 } in
  let tmp138 = (tmp133, tmp137) in
  let tmp139 = (FStarC_Reflection_V2_Constants.fstar_refl_types_lid "universe") in
  let tmp140 = (refl_emb_lid "e_universe") in
  let tmp141 = (nbe_refl_emb_lid "e_universe") in
  let tmp142 = (Some (tmp141)) in
  let tmp143 = { arity = (Prims.parse_int "0"); syn_emb = tmp140; nbe_emb = tmp142 } in
  let tmp144 = (tmp139, tmp143) in
  let tmp145 = (FStarC_Reflection_V2_Constants.fstar_refl_data_lid "vconst") in
  let tmp146 = (refl_emb_lid "e_vconst") in
  let tmp147 = (nbe_refl_emb_lid "e_vconst") in
  let tmp148 = (Some (tmp147)) in
  let tmp149 = { arity = (Prims.parse_int "0"); syn_emb = tmp146; nbe_emb = tmp148 } in
  let tmp150 = (tmp145, tmp149) in
  let tmp151 = (FStarC_Reflection_V2_Constants.fstar_refl_data_lid "aqualv") in
  let tmp152 = (refl_emb_lid "e_aqualv") in
  let tmp153 = (nbe_refl_emb_lid "e_aqualv") in
  let tmp154 = (Some (tmp153)) in
  let tmp155 = { arity = (Prims.parse_int "0"); syn_emb = tmp152; nbe_emb = tmp154 } in
  let tmp156 = (tmp151, tmp155) in
  let tmp157 = (FStarC_Reflection_V2_Constants.fstar_refl_data_lid "pattern") in
  let tmp158 = (refl_emb_lid "e_pattern") in
  let tmp159 = (nbe_refl_emb_lid "e_pattern") in
  let tmp160 = (Some (tmp159)) in
  let tmp161 = { arity = (Prims.parse_int "0"); syn_emb = tmp158; nbe_emb = tmp160 } in
  let tmp162 = (tmp157, tmp161) in
  let tmp163 = (FStarC_Reflection_V2_Constants.fstar_refl_data_lid "namedv_view") in
  let tmp164 = (refl_emb_lid "e_namedv_view") in
  let tmp165 = (nbe_refl_emb_lid "e_namedv_view") in
  let tmp166 = (Some (tmp165)) in
  let tmp167 = { arity = (Prims.parse_int "0"); syn_emb = tmp164; nbe_emb = tmp166 } in
  let tmp168 = (tmp163, tmp167) in
  let tmp169 = (FStarC_Reflection_V2_Constants.fstar_refl_data_lid "bv_view") in
  let tmp170 = (refl_emb_lid "e_bv_view") in
  let tmp171 = (nbe_refl_emb_lid "e_bv_view") in
  let tmp172 = (Some (tmp171)) in
  let tmp173 = { arity = (Prims.parse_int "0"); syn_emb = tmp170; nbe_emb = tmp172 } in
  let tmp174 = (tmp169, tmp173) in
  let tmp175 = (FStarC_Reflection_V2_Constants.fstar_refl_data_lid "binder_view") in
  let tmp176 = (refl_emb_lid "e_binder_view") in
  let tmp177 = (nbe_refl_emb_lid "e_binder_view") in
  let tmp178 = (Some (tmp177)) in
  let tmp179 = { arity = (Prims.parse_int "0"); syn_emb = tmp176; nbe_emb = tmp178 } in
  let tmp180 = (tmp175, tmp179) in
  let tmp181 = (FStarC_Reflection_V2_Constants.fstar_refl_data_lid "binding") in
  let tmp182 = (refl_emb_lid "e_binding") in
  let tmp183 = (nbe_refl_emb_lid "e_binding") in
  let tmp184 = (Some (tmp183)) in
  let tmp185 = { arity = (Prims.parse_int "0"); syn_emb = tmp182; nbe_emb = tmp184 } in
  let tmp186 = (tmp181, tmp185) in
  let tmp187 = (FStarC_Reflection_V2_Constants.fstar_refl_data_lid "universe_view") in
  let tmp188 = (refl_emb_lid "e_universe_view") in
  let tmp189 = (nbe_refl_emb_lid "e_universe_view") in
  let tmp190 = (Some (tmp189)) in
  let tmp191 = { arity = (Prims.parse_int "0"); syn_emb = tmp188; nbe_emb = tmp190 } in
  let tmp192 = (tmp187, tmp191) in
  let tmp193 = (FStarC_Reflection_V2_Constants.fstar_refl_data_lid "term_view") in
  let tmp194 = (refl_emb_lid "e_term_view") in
  let tmp195 = (nbe_refl_emb_lid "e_term_view") in
  let tmp196 = (Some (tmp195)) in
  let tmp197 = { arity = (Prims.parse_int "0"); syn_emb = tmp194; nbe_emb = tmp196 } in
  let tmp198 = (tmp193, tmp197) in
  let tmp199 = (FStarC_Reflection_V2_Constants.fstar_refl_data_lid "comp_view") in
  let tmp200 = (refl_emb_lid "e_comp_view") in
  let tmp201 = (nbe_refl_emb_lid "e_comp_view") in
  let tmp202 = (Some (tmp201)) in
  let tmp203 = { arity = (Prims.parse_int "0"); syn_emb = tmp200; nbe_emb = tmp202 } in
  let tmp204 = (tmp199, tmp203) in
  let tmp205 = (FStarC_Reflection_V2_Constants.fstar_refl_data_lid "cflag") in
  let tmp206 = (refl_emb_lid "e_cflag") in
  let tmp207 = (nbe_refl_emb_lid "e_cflag") in
  let tmp208 = (Some (tmp207)) in
  let tmp209 = { arity = (Prims.parse_int "0"); syn_emb = tmp206; nbe_emb = tmp208 } in
  let tmp210 = (tmp205, tmp209) in
  let tmp211 = (FStarC_Reflection_V2_Constants.fstar_refl_data_lid "decreases_order") in
  let tmp212 = (refl_emb_lid "e_decreases_order") in
  let tmp213 = (nbe_refl_emb_lid "e_decreases_order") in
  let tmp214 = (Some (tmp213)) in
  let tmp215 = { arity = (Prims.parse_int "0"); syn_emb = tmp212; nbe_emb = tmp214 } in
  let tmp216 = (tmp211, tmp215) in
  let tmp217 = (FStarC_Reflection_V2_Constants.fstar_refl_data_lid "lb_view") in
  let tmp218 = (refl_emb_lid "e_lb_view") in
  let tmp219 = (nbe_refl_emb_lid "e_lb_view") in
  let tmp220 = (Some (tmp219)) in
  let tmp221 = { arity = (Prims.parse_int "0"); syn_emb = tmp218; nbe_emb = tmp220 } in
  let tmp222 = (tmp217, tmp221) in
  let tmp223 = (FStarC_Reflection_V2_Constants.fstar_refl_data_lid "sigelt_view") in
  let tmp224 = (refl_emb_lid "e_sigelt_view") in
  let tmp225 = (nbe_refl_emb_lid "e_sigelt_view") in
  let tmp226 = (Some (tmp225)) in
  let tmp227 = { arity = (Prims.parse_int "0"); syn_emb = tmp224; nbe_emb = tmp226 } in
  let tmp228 = (tmp223, tmp227) in
  let tmp229 = (FStarC_Reflection_V2_Constants.fstar_refl_data_lid "qualifier") in
  let tmp230 = (refl_emb_lid "e_qualifier") in
  let tmp231 = (nbe_refl_emb_lid "e_qualifier") in
  let tmp232 = (Some (tmp231)) in
  let tmp233 = { arity = (Prims.parse_int "0"); syn_emb = tmp230; nbe_emb = tmp232 } in
  let tmp234 = (tmp229, tmp233) in
  let tmp235 = (tmp234 :: []) in
  let tmp236 = (tmp228 :: tmp235) in
  let tmp237 = (tmp222 :: tmp236) in
  let tmp238 = (tmp216 :: tmp237) in
  let tmp239 = (tmp210 :: tmp238) in
  let tmp240 = (tmp204 :: tmp239) in
  let tmp241 = (tmp198 :: tmp240) in
  let tmp242 = (tmp192 :: tmp241) in
  let tmp243 = (tmp186 :: tmp242) in
  let tmp244 = (tmp180 :: tmp243) in
  let tmp245 = (tmp174 :: tmp244) in
  let tmp246 = (tmp168 :: tmp245) in
  let tmp247 = (tmp162 :: tmp246) in
  let tmp248 = (tmp156 :: tmp247) in
  let tmp249 = (tmp150 :: tmp248) in
  let tmp250 = (tmp144 :: tmp249) in
  let tmp251 = (tmp138 :: tmp250) in
  let tmp252 = (tmp132 :: tmp251) in
  let tmp253 = (tmp126 :: tmp252) in
  let tmp254 = (tmp120 :: tmp253) in
  let tmp255 = (tmp114 :: tmp254) in
  let tmp256 = (tmp108 :: tmp255) in
  let tmp257 = (tmp102 :: tmp256) in
  let tmp258 = (tmp96 :: tmp257) in
  let tmp259 = (tmp90 :: tmp258) in
  let tmp260 = (tmp84 :: tmp259) in
  let tmp261 = (tmp78 :: tmp260) in
  let tmp262 = (tmp72 :: tmp261) in
  let tmp263 = (tmp66 :: tmp262) in
  let tmp264 = (tmp61 :: tmp263) in
  let tmp265 = (tmp55 :: tmp264) in
  let tmp266 = (tmp49 :: tmp265) in
  let tmp267 = (tmp44 :: tmp266) in
  let tmp268 = (tmp39 :: tmp267) in
  let tmp269 = (tmp34 :: tmp268) in
  let tmp270 = (tmp29 :: tmp269) in
  let tmp271 = (tmp24 :: tmp270) in
  let tmp272 = (tmp19 :: tmp271) in
  let tmp273 = (tmp14 :: tmp272) in
  let tmp274 = (tmp9 :: tmp273) in
  (tmp4 :: tmp274))

let find_fv_embedding' (l : FStarC_Ident.lident) : (embedding_data) option =
  (let tmp = (!(local_fv_embeddings)) in
  let tmp1 = (FStarC_List.op_At tmp builtin_embeddings) in
  let tmp2 = (FStarC_List.find (fun tmp2 -> (match tmp2 with
      | (l', tmp3) -> (FStarC_Ident.lid_equals l l')
    )) tmp1) in
  (match tmp2 with
    | (Some ((tmp3, data))) -> (Some (data))
    | None -> None
  ))

let find_fv_embedding (l : FStarC_Ident.lident) : embedding_data =
  (let tmp = (find_fv_embedding' l) in
  (match tmp with
    | (Some (data)) -> data
    | None -> (raise (NoEmbedding ((Prims.strcat "Embedding not defined for type " (FStarC_Ident.string_of_lid l)))))
  ))

let splitlast (s : ('tmp) list) : (('tmp) list * 'tmp) =
  (let tmp = (FStarC_List.rev s) in
  (match tmp with
    | (x :: xs) -> ((FStarC_List.rev xs), x)
  ))

let ml_name (l : FStarC_Ident.lident) : FStarC_Extraction_ML_Syntax.mlexpr =
  (let s = (FStarC_Ident.path_of_lid l) in
  let tmp = (splitlast s) in
  (match tmp with
    | (ns, id) -> (mk (FStarC_Extraction_ML_Syntax.MLE_Name ((ns, id))))
  ))

let ml_nbe_unsupported : FStarC_Extraction_ML_Syntax.mlexpr =
  (let hd = (mk (FStarC_Extraction_ML_Syntax.MLE_Name ((("FStarC" :: ("TypeChecker" :: ("NBETerm" :: []))), "e_unsupported")))) in
  (mk (FStarC_Extraction_ML_Syntax.MLE_App (hd, (FStarC_Extraction_ML_Syntax.ml_unit :: [])))))

let rec embedding_for (tcenv : FStarC_TypeChecker_Env.env) (mutuals : (FStarC_Ident.lident) list) (k : embedding_kind) (env : ((FStarC_Syntax_Syntax.bv * string)) list) (t : (FStarC_Syntax_Syntax.term') FStarC_Syntax_Syntax.syntax) : FStarC_Extraction_ML_Syntax.mlexpr =
  (let str_to_name = (fun s -> (as_name ([], s))) in
  let emb_arrow = (fun e1 e2 -> (let comb = (match k with
        | SyntaxTerm -> (mk (FStarC_Extraction_ML_Syntax.MLE_Name ((("FStarC" :: ("Syntax" :: ("Embeddings" :: []))), "e_arrow"))))
        | NBETerm -> (mk (FStarC_Extraction_ML_Syntax.MLE_Name ((("FStarC" :: ("TypeChecker" :: ("NBETerm" :: []))), "e_arrow"))))
      ) in
    (mk (FStarC_Extraction_ML_Syntax.MLE_App (comb, (e1 :: (e2 :: []))))))) in
  let find_env_entry = (fun bv tmp -> (match tmp with
      | (bv', tmp1) -> (FStarC_Syntax_Syntax.bv_eq bv bv')
    )) in
  let t1 = (FStarC_TypeChecker_Normalize.unfold_whnf tcenv t) in
  let t2 = (FStarC_Syntax_Util.un_uinst t1) in
  let t3 = (FStarC_Syntax_Subst.compress t2) in
  (match (t3).FStarC_Syntax_Syntax.n with
    | (FStarC_Syntax_Syntax.Tm_name (bv)) when (FStarC_Util.for_some (find_env_entry bv) env) -> (let comb = (match k with
          | SyntaxTerm -> (mk (FStarC_Extraction_ML_Syntax.MLE_Name ((("FStarC" :: ("Syntax" :: ("Embeddings" :: []))), "mk_any_emb"))))
          | NBETerm -> (mk (FStarC_Extraction_ML_Syntax.MLE_Name ((("FStarC" :: ("TypeChecker" :: ("NBETerm" :: []))), "mk_any_emb"))))
        ) in
      let tmp = (FStarC_Option.find (find_env_entry bv) env) in
      let tmp1 = (match tmp with
          | (Some (v)) -> v
        ) in
      let s = (Custard_FStar_Pervasives_Native.fStar_Pervasives_Native_snd tmp1) in
      (mk (FStarC_Extraction_ML_Syntax.MLE_App (comb, ((str_to_name s) :: [])))))
    | (FStarC_Syntax_Syntax.Tm_refine ({ FStarC_Syntax_Syntax.b = x; phi = tmp; _ })) -> (embedding_for tcenv mutuals k env (x).FStarC_Syntax_Syntax.sort)
    | (FStarC_Syntax_Syntax.Tm_ascribed ({ FStarC_Syntax_Syntax.tm = t4; asc = u__1; asc1 = u__2; asc2 = u__3; eff_opt = tmp; _ })) -> (embedding_for tcenv mutuals k env t4)
    | (FStarC_Syntax_Syntax.Tm_arrow (tmp)) when (let tmp1 = (FStarC_Syntax_Util.arrow_one_ln t3) in
    (match tmp1 with
      | (Some ((tmp2, c))) -> (FStarC_Syntax_Util.is_pure_comp c)
      | None -> false
    )) -> (let tmp1 = (FStarC_Syntax_Util.arrow_one t3) in
      let tmp2 = (match tmp1 with
          | (Some (v)) -> v
        ) in
      (match tmp2 with
        | (b, c) -> (let t0 = ((b).FStarC_Syntax_Syntax.binder_bv).FStarC_Syntax_Syntax.sort in
          let t11 = (FStarC_Syntax_Util.comp_result c) in
          let tmp3 = (embedding_for tcenv mutuals k env t0) in
          let tmp4 = (embedding_for tcenv mutuals k env t11) in
          (emb_arrow tmp3 tmp4))
      ))
    | (FStarC_Syntax_Syntax.Tm_app (tmp)) -> (let tmp1 = (FStarC_Syntax_Util.head_and_args_full t3) in
      (match tmp1 with
        | (head, args) -> (let e_head = (embedding_for tcenv mutuals k env head) in
          let e_args = (FStarC_List.map (fun tmp2 -> (match tmp2 with
              | (t4, tmp3) -> (embedding_for tcenv mutuals k env t4)
            )) args) in
          (mk (FStarC_Extraction_ML_Syntax.MLE_App (e_head, e_args))))
      ))
    | (FStarC_Syntax_Syntax.Tm_fvar (fv)) when (FStarC_List.existsb (FStarC_Ident.lid_equals (fv).FStarC_Syntax_Syntax.fv_name) mutuals) -> (let head = (mk (FStarC_Extraction_ML_Syntax.MLE_Var ((Prims.strcat "__knot_e_" (FStarC_Ident.string_of_id (FStarC_Ident.ident_of_lid (fv).FStarC_Syntax_Syntax.fv_name)))))) in
      (mk (FStarC_Extraction_ML_Syntax.MLE_App (head, (FStarC_Extraction_ML_Syntax.ml_unit :: [])))))
    | (FStarC_Syntax_Syntax.Tm_fvar (fv)) when (let tmp = (find_fv_embedding' (fv).FStarC_Syntax_Syntax.fv_name) in
    (match tmp with
      | (Some (v)) -> true
      | tmp1 -> false
    )) -> (let emb_data = (find_fv_embedding (fv).FStarC_Syntax_Syntax.fv_name) in
      (match k with
        | SyntaxTerm -> (ml_name (emb_data).syn_emb)
        | NBETerm -> (match (emb_data).nbe_emb with
            | (Some (lid)) -> (ml_name lid)
            | None -> ml_nbe_unsupported
          )
      ))
    | (FStarC_Syntax_Syntax.Tm_fvar (fv)) when (FStarC_TypeChecker_Env.fv_has_attr tcenv fv FStarC_Parser_Const.plugin_attr) -> (match k with
        | SyntaxTerm -> (let lid = (fv).FStarC_Syntax_Syntax.fv_name in
          let tmp = (FStarC_List.map FStarC_Ident.string_of_id (FStarC_Ident.ns_of_lid lid)) in
          let tmp1 = (tmp, (Prims.strcat "e_" (FStarC_Ident.string_of_id (FStarC_Ident.ident_of_lid lid)))) in
          (as_name tmp1))
        | NBETerm -> ml_nbe_unsupported
      )
    | (FStarC_Syntax_Syntax.Tm_fvar (fv)) -> (let tmp = (FStarC_Syntax_Print.fStarC_Class_Show_show__syntax_term' t3) in
      let tmp1 = (FStarC_Format.fmt1 "Embedding not defined for name `%s'" tmp) in
      let tmp2 = (NoEmbedding (tmp1)) in
      (raise tmp2))
    | tmp -> (let tmp1 = (FStarC_Syntax_Print.fStarC_Class_Show_show__syntax_term' t3) in
      let tmp2 = (FStarC_Syntax_Syntax.fStarC_Class_Tagged_tag_of__syntax_term' t3) in
      let tmp3 = (FStarC_Format.fmt2 "Cannot embed type `%s' (%s)" tmp1 tmp2) in
      let tmp4 = (NoEmbedding (tmp3)) in
      (raise tmp4))
  ))

let rec interpret_plugin_as_term_fun__aux (fv : FStarC_Syntax_Syntax.fv) (tcenv : FStarC_TypeChecker_Env.env) (t : (FStarC_Syntax_Syntax.term') FStarC_Syntax_Syntax.syntax) (lid_to_name : (FStarC_Ident.lident -> FStarC_Extraction_ML_Syntax.mlexpr)) (str_to_name : (string -> FStarC_Extraction_ML_Syntax.mlexpr)) (fv_lid_embedded : FStarC_Extraction_ML_Syntax.mlexpr) (mk_tactic_interpretation : (embedding_kind -> (Prims.int -> FStarC_Extraction_ML_Syntax.mlexpr))) (mk_from_tactic : (embedding_kind -> (Prims.int -> FStarC_Extraction_ML_Syntax.mlexpr))) (mk_arrow_as_prim_step : (embedding_kind -> (Prims.int -> FStarC_Extraction_ML_Syntax.mlexpr))) (abstract_tvars : ((string) list -> (FStarC_Extraction_ML_Syntax.mlexpr -> FStarC_Extraction_ML_Syntax.mlexpr))) (c : (FStarC_Syntax_Syntax.comp_typ) FStarC_Syntax_Syntax.syntax) (result_typ : (FStarC_Syntax_Syntax.term') FStarC_Syntax_Syntax.syntax) (arity : Prims.int) (non_tvar_arity : Prims.int) (tvar_names : (string) list) (tvar_context : ((FStarC_Syntax_Syntax.bv * string)) list) (loc : embedding_kind) (accum_embeddings : (FStarC_Extraction_ML_Syntax.mlexpr) list) (bs : (FStarC_Syntax_Syntax.binder) list) : (FStarC_Extraction_ML_Syntax.mlexpr * Prims.int * bool) =
  (match bs with
    | [] -> (let arg_unembeddings = (FStarC_List.rev accum_embeddings) in
      let res_embedding = (embedding_for tcenv [] loc tvar_context result_typ) in
      let fv_lid = (fv).FStarC_Syntax_Syntax.fv_name in
      let tmp = (FStarC_Syntax_Util.is_pure_comp c) in
      (if tmp then (let cb = (str_to_name "cb") in
      let us = (str_to_name "us") in
      let embed_fun_N = (mk_arrow_as_prim_step loc non_tvar_arity) in
      let tmp1 = (lid_to_name fv_lid) in
      let tmp2 = (tmp1 :: (fv_lid_embedded :: (cb :: (us :: [])))) in
      let tmp3 = (res_embedding :: tmp2) in
      let args = (FStarC_List.op_At arg_unembeddings tmp3) in
      let fun_embedding = (mk (FStarC_Extraction_ML_Syntax.MLE_App (embed_fun_N, args))) in
      let tabs = (abstract_tvars tvar_names fun_embedding) in
      let cb_tabs = (ml_lam "cb" (ml_lam "us" tabs)) in
      ((if ((=) loc NBETerm) then cb_tabs else (ml_lam "_psc" cb_tabs)), arity, true)) else (if (FStarC_Ident.lid_equals (FStarC_Syntax_Util.comp_effect_name c) FStarC_Parser_Const.effect_TAC_lid) then (let h = (mk_tactic_interpretation loc non_tvar_arity) in
      let tmp1 = (mk_from_tactic loc non_tvar_arity) in
      let tmp2 = (lid_to_name fv_lid) in
      let tmp3 = (tmp2 :: []) in
      let tmp4 = (tmp1, tmp3) in
      let tmp5 = (FStarC_Extraction_ML_Syntax.MLE_App ((match tmp4 with (custard_tup, _) -> custard_tup), (match tmp4 with (_, custard_tup) -> custard_tup))) in
      let tac_fun = (mk tmp5) in
      let psc = (str_to_name "psc") in
      let ncb = (str_to_name "ncb") in
      let us = (str_to_name "us") in
      let all_args = (str_to_name "args") in
      let args = (FStarC_List.op_At ((mk (FStarC_Extraction_ML_Syntax.MLE_Const ((FStarC_Extraction_ML_Syntax.MLC_String ((Prims.strcat (FStarC_Ident.string_of_lid fv_lid) " (plugin)")))))) :: []) (FStarC_List.op_At (tac_fun :: []) (FStarC_List.op_At arg_unembeddings (res_embedding :: (psc :: (ncb :: (us :: []))))))) in
      let tabs = (match tvar_names with
          | [] -> (ml_lam "args" (mk (FStarC_Extraction_ML_Syntax.MLE_App (h, (FStarC_List.op_At args (all_args :: []))))))
          | tmp6 -> (abstract_tvars tvar_names (mk (FStarC_Extraction_ML_Syntax.MLE_App (h, args))))
        ) in
      ((ml_lam "psc" (ml_lam "ncb" (ml_lam "us" tabs))), (Prims.op_Plus arity (Prims.parse_int "1")), false)) else (let tmp1 = (FStarC_Syntax_Print.fStarC_Class_Show_show__syntax_term' t) in
      let tmp2 = (Prims.strcat "Plugins not defined for type " tmp1) in
      let tmp3 = (NoEmbedding (tmp2)) in
      (raise tmp3)))))
    | ({ FStarC_Syntax_Syntax.binder_bv = b; binder_qual = tmp; binder_positivity = tmp1; binder_attrs = tmp2; _ } :: bs1) -> (let emb = (embedding_for tcenv [] loc tvar_context (b).FStarC_Syntax_Syntax.sort) in
      ((interpret_plugin_as_term_fun__aux fv tcenv t lid_to_name str_to_name fv_lid_embedded mk_tactic_interpretation mk_from_tactic mk_arrow_as_prim_step abstract_tvars c result_typ arity non_tvar_arity tvar_names tvar_context) loc (emb :: accum_embeddings) bs1))
  )

let not_implemented_warning (r : FStarC_Range_Type.range) (t : string) (msg : string) : unit =
  (let tmp = (FStarC_Errors.lookup FStarC_Errors_Codes.Warning_PluginNotImplemented) in
  let tmp1 = (FStarC_Errors.error_number tmp) in
  let tmp2 = (FStarC_Class_PP.fStarC_Class_PP_pp__int tmp1) in
  let tmp3 = (FStar_Pprint.op_Hat_Slash_Hat tmp2 (FStarC_Errors_Msg.text "to carry on.")) in
  let tmp4 = (FStar_Pprint.op_Hat_Hat (FStarC_Errors_Msg.text "Use --warn_error -") tmp3) in
  let tmp5 = (tmp4 :: []) in
  let tmp6 = ((FStar_Pprint.prefix (Prims.parse_int "2") (Prims.parse_int "1") (FStarC_Errors_Msg.text (FStarC_Format.fmt1 "Plugin `%s' can not run natively because:" t)) (FStarC_Errors_Msg.text msg)) :: tmp5) in
  (FStarC_Errors.fStarC_Errors_log_issue__range_list_document r FStarC_Errors_Codes.Warning_PluginNotImplemented tmp6))

let interpret_plugin_as_term_fun (env : FStarC_Extraction_ML_UEnv.uenv) (fv : FStarC_Syntax_Syntax.fv) (t : (FStarC_Syntax_Syntax.term') FStarC_Syntax_Syntax.syntax) (arity : (Prims.int) option) (ml_fv : FStarC_Extraction_ML_Syntax.mlexpr') : ((FStarC_Extraction_ML_Syntax.mlexpr * FStarC_Extraction_ML_Syntax.mlexpr * Prims.int * bool)) option =
  (let fv_lid = (fv).FStarC_Syntax_Syntax.fv_name in
  let tcenv = (FStarC_Extraction_ML_UEnv.tcenv_of_uenv env) in
  let t1 = (FStarC_TypeChecker_Normalize.normalize (FStarC_TypeChecker_Env.EraseUniverses :: (FStarC_TypeChecker_Env.AllowUnboundUniverses :: ((FStarC_TypeChecker_Env.UnfoldUntil (FStarC_Syntax_Syntax.delta_constant)) :: (FStarC_TypeChecker_Env.ForExtraction :: [])))) tcenv t) in
  let as_name_ = (fun mlp -> (FStarC_Extraction_ML_Syntax.with_ty FStarC_Extraction_ML_Syntax.MLTY_Top (FStarC_Extraction_ML_Syntax.MLE_Name (mlp)))) in
  let lid_to_name = (fun l -> (let tmp = (FStarC_Extraction_ML_UEnv.mlpath_of_lident env l) in
    let tmp1 = (FStarC_Extraction_ML_Syntax.MLE_Name (tmp)) in
    (FStarC_Extraction_ML_Syntax.with_ty FStarC_Extraction_ML_Syntax.MLTY_Top tmp1))) in
  let str_to_name = (fun s -> (as_name_ ([], s))) in
  let fv_lid_embedded = (FStarC_Extraction_ML_Syntax.with_ty FStarC_Extraction_ML_Syntax.MLTY_Top (FStarC_Extraction_ML_Syntax.MLE_App ((as_name_ (("FStarC_Ident" :: []), "lid_of_str")), ((FStarC_Extraction_ML_Syntax.with_ty FStarC_Extraction_ML_Syntax.MLTY_Top (FStarC_Extraction_ML_Syntax.MLE_Const ((FStarC_Extraction_ML_Syntax.MLC_String ((FStarC_Ident.string_of_lid fv_lid)))))) :: [])))) in
  let mk_tactic_interpretation = (fun l arity1 -> (if (Prims.op_Greater arity1 FStarC_Tactics_InterpFuns.max_tac_arity) then (raise (NoEmbedding ("tactic plugins can only take up to 20 arguments"))) else (let idroot = (match l with
        | SyntaxTerm -> "mk_tactic_interpretation_"
        | NBETerm -> "mk_nbe_tactic_interpretation_"
      ) in
    let tmp = (FStarC_Class_Show.fStarC_Class_Show_show__int arity1) in
    let tmp1 = (Prims.strcat idroot tmp) in
    let tmp2 = (("FStarC_Tactics_InterpFuns" :: []), tmp1) in
    (as_name_ tmp2)))) in
  let mk_from_tactic = (fun l arity1 -> (let idroot = (match l with
        | SyntaxTerm -> "from_tactic_"
        | NBETerm -> "from_nbe_tactic_"
      ) in
    let tmp = (FStarC_Class_Show.fStarC_Class_Show_show__int_4 arity1) in
    let tmp1 = (Prims.strcat idroot tmp) in
    let tmp2 = (("FStarC_Tactics_Native" :: []), tmp1) in
    (as_name_ tmp2))) in
  let mk_arrow_as_prim_step = (fun k arity1 -> (let modul = (match k with
        | SyntaxTerm -> ("FStarC" :: ("Syntax" :: ("Embeddings" :: [])))
        | NBETerm -> ("FStarC" :: ("TypeChecker" :: ("NBETerm" :: [])))
      ) in
    let tmp = (FStarC_Class_Show.fStarC_Class_Show_show__int arity1) in
    let tmp1 = (Prims.strcat "arrow_as_prim_step_" tmp) in
    let tmp2 = (modul, tmp1) in
    (as_name_ tmp2))) in
  let abstract_tvars = (fun tvar_names body -> (match tvar_names with
      | [] -> (let body1 = (mk (FStarC_Extraction_ML_Syntax.MLE_App ((as_name_ (("FStarC_Syntax_Embeddings" :: []), "debug_wrap")), ((FStarC_Extraction_ML_Syntax.with_ty FStarC_Extraction_ML_Syntax.MLTY_Top (FStarC_Extraction_ML_Syntax.MLE_Const ((FStarC_Extraction_ML_Syntax.MLC_String ((FStarC_Ident.string_of_lid fv_lid)))))) :: ((ml_lam "_" (mk (FStarC_Extraction_ML_Syntax.MLE_App (body, ((str_to_name "args") :: []))))) :: []))))) in
        (ml_lam "args" body1))
      | tmp -> (let args_tail = (FStarC_Extraction_ML_Syntax.MLP_Var ("args_tail")) in
        let mk_cons = (fun hd_pat tail_pat -> (FStarC_Extraction_ML_Syntax.MLP_CTor ((("Prims" :: []), "Cons"), (hd_pat :: (tail_pat :: []))))) in
        let fst_pat = (fun v -> (FStarC_Extraction_ML_Syntax.MLP_Tuple (((FStarC_Extraction_ML_Syntax.MLP_Var (v)) :: (FStarC_Extraction_ML_Syntax.MLP_Wild :: []))))) in
        let pattern = (FStarC_List.fold_right (fun hd_var -> (mk_cons (fst_pat hd_var))) tvar_names args_tail) in
        let branch = (pattern, None, (mk (FStarC_Extraction_ML_Syntax.MLE_App (body, ((as_name_ ([], "args_tail")) :: []))))) in
        let default_branch = (FStarC_Extraction_ML_Syntax.MLP_Wild, None, (mk (FStarC_Extraction_ML_Syntax.MLE_App ((str_to_name "failwith"), ((mk (FStarC_Extraction_ML_Syntax.MLE_Const ((FStarC_Extraction_ML_Syntax.MLC_String ("arity mismatch"))))) :: []))))) in
        let body1 = (mk (FStarC_Extraction_ML_Syntax.MLE_Match ((as_name_ ([], "args")), (branch :: (default_branch :: []))))) in
        let body2 = (mk (FStarC_Extraction_ML_Syntax.MLE_App ((as_name_ (("FStarC_Syntax_Embeddings" :: []), "debug_wrap")), ((FStarC_Extraction_ML_Syntax.with_ty FStarC_Extraction_ML_Syntax.MLTY_Top (FStarC_Extraction_ML_Syntax.MLE_Const ((FStarC_Extraction_ML_Syntax.MLC_String ((FStarC_Ident.string_of_lid fv_lid)))))) :: ((ml_lam "_" body1) :: []))))) in
        (ml_lam "args" body2))
    )) in
  let tmp = (FStarC_Syntax_Util.arrow_formals_comp t1) in
  (match tmp with
    | (bs, c) -> (let tmp1 = (match arity with
          | None -> (bs, c)
          | (Some (n)) -> (let n_bs = (FStarC_List.length bs) in
            (if ((=) n n_bs) then (bs, c) else (if (Prims.op_Less n n_bs) then (let tmp1 = (FStarC_Util.first_N n bs) in
            (match tmp1 with
              | (bs1, rest) -> (let tmp2 = (FStarC_Syntax_Util.arrow rest c) in
                let c1 = (FStarC_Syntax_Syntax.mk_Total tmp2) in
                (bs1, c1))
            )) else (let tmp1 = (FStarC_Class_Show.fStarC_Class_Show_show__int n) in
            let tmp2 = (FStarC_Class_Show.fStarC_Class_Show_show__int_4 n_bs) in
            let msg = (FStarC_Format.fmt3 "Embedding not defined for %s; expected arity at least %s; got %s" (FStarC_Ident.string_of_lid fv_lid) tmp1 tmp2) in
            (raise (NoEmbedding (msg)))))))
        ) in
      (match tmp1 with
        | (bs1, c1) -> (let result_typ = (FStarC_Syntax_Util.comp_result c1) in
          let arity1 = (FStarC_List.length bs1) in
          let tmp2 = (FStarC_Util.prefix_until (fun tmp2 -> (let tmp3 = (FStarC_Syntax_Subst.compress ((tmp2).FStarC_Syntax_Syntax.binder_bv).FStarC_Syntax_Syntax.sort) in
            let tmp4 = (tmp3).FStarC_Syntax_Syntax.n in
            (match tmp4 with
              | (FStarC_Syntax_Syntax.Tm_type (tmp5)) -> false
              | tmp5 -> true
            ))) bs1) in
          let tmp3 = (match tmp2 with
              | None -> (bs1, [])
              | (Some ((tvars, x, rest))) -> (tvars, (x :: rest))
            ) in
          (match tmp3 with
            | (type_vars, bs2) -> (let non_tvar_arity = (FStarC_List.length bs2) in
              let tvar_names = (FStarC_List.mapi (fun i tv -> (let tmp4 = (FStarC_Class_Show.fStarC_Class_Show_show__int i) in
                (Prims.strcat "tv_" tmp4))) type_vars) in
              let tvar_context = (FStarC_List.map2 (fun b nm -> ((b).FStarC_Syntax_Syntax.binder_bv, nm)) type_vars tvar_names) in
              (try (let tmp4 = ((interpret_plugin_as_term_fun__aux fv tcenv t1 lid_to_name str_to_name fv_lid_embedded mk_tactic_interpretation mk_from_tactic mk_arrow_as_prim_step abstract_tvars c1 result_typ arity1 non_tvar_arity tvar_names tvar_context) SyntaxTerm [] bs2) in
              (match tmp4 with
                | (w, a, b) -> (let tmp5 = ((interpret_plugin_as_term_fun__aux fv tcenv t1 lid_to_name str_to_name fv_lid_embedded mk_tactic_interpretation mk_from_tactic mk_arrow_as_prim_step abstract_tvars c1 result_typ arity1 non_tvar_arity tvar_names tvar_context) NBETerm [] bs2) in
                  (match tmp5 with
                    | (w', tmp6, tmp7) -> (Some ((w, w', a, b)))
                  ))
              )) with
                | (NoEmbedding (msg)) -> (let tmp4 = (FStarC_Syntax_Syntax.fStarC_Class_Show_show__fv fv) in
                  (not_implemented_warning (FStarC_Ident.range_of_lid (fv).FStarC_Syntax_Syntax.fv_name) tmp4 msg);
                  None)
              ))
          ))
      ))
  ))

exception Unsupported of string

let fresh : (string -> string) =
  (let r = (ref (Prims.parse_int "0")) in
  (fun s -> (let v = (!(r)) in
  ((r) := (Prims.op_Plus v (Prims.parse_int "1")));
  let tmp = (FStarC_Class_Show.fStarC_Class_Show_show__int v) in
  let tmp1 = (Prims.strcat "_" tmp) in
  (Prims.strcat s tmp1))))

let rec pats_to_list_pat (vs : (FStarC_Extraction_ML_Syntax.mlpattern) list) : FStarC_Extraction_ML_Syntax.mlpattern =
  (match vs with
    | [] -> (FStarC_Extraction_ML_Syntax.MLP_CTor ((("Prims" :: []), "Nil"), []))
    | (p :: ps) -> (FStarC_Extraction_ML_Syntax.MLP_CTor ((("Prims" :: []), "Cons"), (p :: ((pats_to_list_pat ps) :: []))))
  )

let ml_record (l : FStarC_Ident.lident) (args : ((string * FStarC_Extraction_ML_Syntax.mlexpr)) list) : FStarC_Extraction_ML_Syntax.mlexpr =
  ((ignore (FStarC_Ident.path_of_lid l));
  (mk (FStarC_Extraction_ML_Syntax.MLE_Record ([], (FStarC_Ident.string_of_id (FStarC_Ident.ident_of_lid l)), args))))

let ml_ctor (l : FStarC_Ident.lident) (args : (FStarC_Extraction_ML_Syntax.mlexpr) list) : FStarC_Extraction_ML_Syntax.mlexpr =
  (let s = (FStarC_Ident.path_of_lid l) in
  let tmp = (splitlast s) in
  (match tmp with
    | (ns, id) -> (mk (FStarC_Extraction_ML_Syntax.MLE_CTor ((ns, id), args)))
  ))

let ml_some : FStarC_Extraction_ML_Syntax.mlexpr =
  (mk (FStarC_Extraction_ML_Syntax.MLE_Name ((("FStar" :: ("Pervasives" :: ("Native" :: []))), "Some"))))

let unembed : FStarC_Extraction_ML_Syntax.mlexpr =
  (mk (FStarC_Extraction_ML_Syntax.MLE_Name ((splitlast ("FStarC" :: ("Syntax" :: ("Embeddings" :: ("Base" :: ("extracted_unembed" :: [])))))))))

let bind_opt : FStarC_Extraction_ML_Syntax.mlexpr =
  (mk (FStarC_Extraction_ML_Syntax.MLE_Name ((splitlast ("FStarC" :: ("Option" :: ("bind" :: [])))))))

let ml_none : FStarC_Extraction_ML_Syntax.mlexpr =
  (mk (FStarC_Extraction_ML_Syntax.MLE_Name ((("FStar" :: ("Pervasives" :: ("Native" :: []))), "None"))))

let mk_unembed (tcenv : FStarC_TypeChecker_Env.env) (mutuals : (FStarC_Ident.lident) list) (record_fields : ((((string) list * string)) list) option) (ctors : (FStarC_Syntax_Syntax.sigelt) list) : FStarC_Extraction_ML_Syntax.mlexpr =
  (let e_branches = (ref []) in
  let arg_v = (fresh "tm") in
  (FStarC_List.iter (fun ctor -> (match (ctor).FStarC_Syntax_Syntax.sigel with
    | (FStarC_Syntax_Syntax.Sig_datacon ({ FStarC_Syntax_Syntax.lid = lid; us = us; t = t; ty_lid = ty_lid; num_ty_params = num_ty_params; mutuals = tmp; injective_type_params = tmp1; proj_disc_lids = tmp2; _ })) -> ((ignore (fresh "fv"));
      let tmp3 = (FStarC_Syntax_Util.arrow_formals t) in
      (match tmp3 with
        | (bs, c) -> (let vs = (FStarC_List.map (fun b -> (let tmp4 = (fresh (FStarC_Ident.string_of_id ((b).FStarC_Syntax_Syntax.binder_bv).FStarC_Syntax_Syntax.ppname)) in
            (tmp4, ((b).FStarC_Syntax_Syntax.binder_bv).FStarC_Syntax_Syntax.sort))) bs) in
          let pat_s = (FStarC_Extraction_ML_Syntax.MLP_Const ((FStarC_Extraction_ML_Syntax.MLC_String ((FStarC_Ident.string_of_lid lid))))) in
          let tmp4 = (FStarC_List.map (fun tmp4 -> (match tmp4 with
              | (v, tmp5) -> (FStarC_Extraction_ML_Syntax.MLP_Var (v))
            )) vs) in
          let pat_args = (pats_to_list_pat tmp4) in
          let pat_both = (FStarC_Extraction_ML_Syntax.MLP_Tuple ((pat_s :: (pat_args :: [])))) in
          let ret = (match record_fields with
              | (Some (fields)) -> (let tmp5 = (FStarC_List.map2 (fun tmp5 fld -> (match tmp5 with
                    | (v, tmp6) -> ((Custard_FStar_Pervasives_Native.fStar_Pervasives_Native_snd fld), (mk (FStarC_Extraction_ML_Syntax.MLE_Var (v))))
                  )) vs fields) in
                (ml_record lid tmp5))
              | None -> (let tmp5 = (FStarC_List.map (fun tmp5 -> (match tmp5 with
                    | (v, tmp6) -> (mk (FStarC_Extraction_ML_Syntax.MLE_Var (v)))
                  )) vs) in
                (ml_ctor lid tmp5))
            ) in
          let ret1 = (mk (FStarC_Extraction_ML_Syntax.MLE_App (ml_some, (ret :: [])))) in
          let body = (FStarC_List.fold_right (fun tmp5 body -> (match tmp5 with
              | (v, ty) -> (let body1 = (mk (FStarC_Extraction_ML_Syntax.MLE_Fun (((mk_binder v FStarC_Extraction_ML_Syntax.MLTY_Top) :: []), body))) in
                let tmp6 = (embedding_for tcenv mutuals SyntaxTerm [] ty) in
                let tmp7 = (tmp6 :: ((mk (FStarC_Extraction_ML_Syntax.MLE_Var (v))) :: [])) in
                let tmp8 = (unembed, tmp7) in
                let tmp9 = (FStarC_Extraction_ML_Syntax.MLE_App ((match tmp8 with (custard_tup, _) -> custard_tup), (match tmp8 with (_, custard_tup) -> custard_tup))) in
                let tmp10 = (mk tmp9) in
                let tmp11 = (tmp10 :: (body1 :: [])) in
                let tmp12 = (bind_opt, tmp11) in
                let tmp13 = (FStarC_Extraction_ML_Syntax.MLE_App ((match tmp12 with (custard_tup, _) -> custard_tup), (match tmp12 with (_, custard_tup) -> custard_tup))) in
                (mk tmp13))
            )) vs ret1) in
          let br = (pat_both, None, body) in
          let tmp5 = (!(e_branches)) in
          let tmp6 = (br :: tmp5) in
          ((e_branches) := tmp6))
      ))
    | tmp -> (FStarC_Effect.failwith "impossible, filter above")
  )) ctors);
  let nomatch = (FStarC_Extraction_ML_Syntax.MLP_Wild, None, ml_none) in
  let tmp = (!(e_branches)) in
  let tmp1 = (nomatch :: tmp) in
  let branches = (FStarC_List.rev tmp1) in
  let sc = (mk (FStarC_Extraction_ML_Syntax.MLE_Var (arg_v))) in
  let def = (mk (FStarC_Extraction_ML_Syntax.MLE_Match (sc, branches))) in
  (mk (FStarC_Extraction_ML_Syntax.MLE_Fun (((mk_binder arg_v FStarC_Extraction_ML_Syntax.MLTY_Top) :: []), def))))

let s_tdataconstr : FStarC_Extraction_ML_Syntax.mlexpr =
  (mk (FStarC_Extraction_ML_Syntax.MLE_Name ((splitlast ("FStarC" :: ("Syntax" :: ("Syntax" :: ("tdataconstr" :: []))))))))

let lid_of_str : FStarC_Extraction_ML_Syntax.mlexpr =
  (mk (FStarC_Extraction_ML_Syntax.MLE_Name ((splitlast ("FStarC" :: ("Ident" :: ("lid_of_str" :: [])))))))

let nil_lid : FStarC_Ident.lident =
  (FStarC_Ident.lid_of_str "Prims.Nil")

let cons_lid : FStarC_Ident.lident =
  (FStarC_Ident.lid_of_str "Prims.Cons")

let rec as_ml_list (ts : (FStarC_Extraction_ML_Syntax.mlexpr) list) : FStarC_Extraction_ML_Syntax.mlexpr =
  (match ts with
    | [] -> (ml_ctor nil_lid [])
    | (t :: ts1) -> (let tmp = (as_ml_list ts1) in
      let tmp1 = (tmp :: []) in
      let tmp2 = (t :: tmp1) in
      (ml_ctor cons_lid tmp2))
  )

let mk_app : FStarC_Extraction_ML_Syntax.mlexpr =
  (mk (FStarC_Extraction_ML_Syntax.MLE_Name ((splitlast ("FStarC" :: ("Syntax" :: ("Util" :: ("mk_app" :: []))))))))

let embed : FStarC_Extraction_ML_Syntax.mlexpr =
  (mk (FStarC_Extraction_ML_Syntax.MLE_Name ((splitlast ("FStarC" :: ("Syntax" :: ("Embeddings" :: ("Base" :: ("extracted_embed" :: [])))))))))

let mk_embed (tcenv : FStarC_TypeChecker_Env.env) (mutuals : (FStarC_Ident.lident) list) (record_fields : ((((string) list * string)) list) option) (ctors : (FStarC_Syntax_Syntax.sigelt) list) : FStarC_Extraction_ML_Syntax.mlexpr =
  (let e_branches = (ref []) in
  let arg_v = (fresh "tm") in
  (FStarC_List.iter (fun ctor -> (match (ctor).FStarC_Syntax_Syntax.sigel with
    | (FStarC_Syntax_Syntax.Sig_datacon ({ FStarC_Syntax_Syntax.lid = lid; us = us; t = t; ty_lid = ty_lid; num_ty_params = num_ty_params; mutuals = tmp; injective_type_params = tmp1; proj_disc_lids = tmp2; _ })) -> ((ignore (fresh "fv"));
      let tmp3 = (FStarC_Syntax_Util.arrow_formals t) in
      (match tmp3 with
        | (bs, c) -> (let vs = (FStarC_List.map (fun b -> (let tmp4 = (fresh (FStarC_Ident.string_of_id ((b).FStarC_Syntax_Syntax.binder_bv).FStarC_Syntax_Syntax.ppname)) in
            (tmp4, ((b).FStarC_Syntax_Syntax.binder_bv).FStarC_Syntax_Syntax.sort))) bs) in
          let pat = (match record_fields with
              | (Some (fields)) -> (let tmp4 = (FStarC_List.map2 (fun v fld -> ((Custard_FStar_Pervasives_Native.fStar_Pervasives_Native_snd fld), (FStarC_Extraction_ML_Syntax.MLP_Var ((Custard_FStar_Pervasives_Native.fStar_Pervasives_Native_fst v))))) vs fields) in
                let tmp5 = ([], tmp4) in
                (FStarC_Extraction_ML_Syntax.MLP_Record ((match tmp5 with (custard_tup, _) -> custard_tup), (match tmp5 with (_, custard_tup) -> custard_tup))))
              | None -> (let tmp4 = (FStarC_Ident.path_of_lid lid) in
                let tmp5 = (splitlast tmp4) in
                let tmp6 = (FStarC_List.map (fun v -> (FStarC_Extraction_ML_Syntax.MLP_Var ((Custard_FStar_Pervasives_Native.fStar_Pervasives_Native_fst v)))) vs) in
                let tmp7 = (tmp5, tmp6) in
                (FStarC_Extraction_ML_Syntax.MLP_CTor ((match tmp7 with (custard_tup, _) -> custard_tup), (match tmp7 with (_, custard_tup) -> custard_tup))))
            ) in
          let fvar = s_tdataconstr in
          let lid_of_str_ = lid_of_str in
          let head = (mk (FStarC_Extraction_ML_Syntax.MLE_App (fvar, ((mk (FStarC_Extraction_ML_Syntax.MLE_App (lid_of_str_, ((mk (FStarC_Extraction_ML_Syntax.MLE_Const ((FStarC_Extraction_ML_Syntax.MLC_String ((FStarC_Ident.string_of_lid lid)))))) :: [])))) :: [])))) in
          let mk_mk_app = (fun t1 ts -> (let ts1 = (FStarC_List.map (fun t2 -> (mk (FStarC_Extraction_ML_Syntax.MLE_Tuple ((t2 :: (ml_none :: [])))))) ts) in
            let tmp4 = (as_ml_list ts1) in
            let tmp5 = (tmp4 :: []) in
            let tmp6 = (t1 :: tmp5) in
            let tmp7 = (mk_app, tmp6) in
            let tmp8 = (FStarC_Extraction_ML_Syntax.MLE_App ((match tmp7 with (custard_tup, _) -> custard_tup), (match tmp7 with (_, custard_tup) -> custard_tup))) in
            (mk tmp8))) in
          let args = (FStarC_List.map (fun tmp4 -> (match tmp4 with
              | (v, ty) -> (let vt = (mk (FStarC_Extraction_ML_Syntax.MLE_Var (v))) in
                let tmp5 = (embedding_for tcenv mutuals SyntaxTerm [] ty) in
                let tmp6 = (tmp5 :: (vt :: [])) in
                let tmp7 = (embed, tmp6) in
                let tmp8 = (FStarC_Extraction_ML_Syntax.MLE_App ((match tmp7 with (custard_tup, _) -> custard_tup), (match tmp7 with (_, custard_tup) -> custard_tup))) in
                (mk tmp8))
            )) vs) in
          let ret = (mk_mk_app head args) in
          let br = (pat, None, ret) in
          let tmp4 = (!(e_branches)) in
          let tmp5 = (br :: tmp4) in
          ((e_branches) := tmp5))
      ))
    | tmp -> (FStarC_Effect.failwith "impossible, filter above")
  )) ctors);
  let tmp = (!(e_branches)) in
  let branches = (FStarC_List.rev tmp) in
  let sc = (mk (FStarC_Extraction_ML_Syntax.MLE_Var (arg_v))) in
  let def = (mk (FStarC_Extraction_ML_Syntax.MLE_Match (sc, branches))) in
  (mk (FStarC_Extraction_ML_Syntax.MLE_Fun (((mk_binder arg_v FStarC_Extraction_ML_Syntax.MLTY_Top) :: []), def))))

let dbg_plugin : (bool ref) =
  (FStarC_Debug.get_toggle "Plugins")

let register_embedding (l : FStarC_Ident.lident) (d : embedding_data) : unit =
  (let tmp = (!(dbg_plugin)) in
  (if tmp then (FStarC_Format.print1 "Registering local embedding for %s\n" (FStarC_Ident.string_of_lid l)) else ());
  let tmp1 = (!(local_fv_embeddings)) in
  let tmp2 = ((l, d) :: tmp1) in
  ((local_fv_embeddings) := tmp2))

let u___do_handle_plugin (g : FStarC_Extraction_ML_UEnv.uenv) (arity_opt : (Prims.int) option) (se : FStarC_Syntax_Syntax.sigelt) : (FStarC_Extraction_ML_Syntax.mlmodule1) list =
  (match (se).FStarC_Syntax_Syntax.sigel with
    | (FStarC_Syntax_Syntax.Sig_let ({ FStarC_Syntax_Syntax.lbs = lbs; lids = tmp; _ })) -> (let mk_registration = (fun lb -> (let fv = (match (lb).FStarC_Syntax_Syntax.lbname with
            | (FStar_Pervasives.Inr (v)) -> v
          ) in
        let fv_lid = (fv).FStarC_Syntax_Syntax.fv_name in
        let fv_t = (lb).FStarC_Syntax_Syntax.lbtyp in
        let ml_name_str = (FStarC_Extraction_ML_Syntax.MLE_Const ((FStarC_Extraction_ML_Syntax.MLC_String ((FStarC_Ident.string_of_lid fv_lid))))) in
        let tmp1 = (interpret_plugin_as_term_fun g fv fv_t arity_opt ml_name_str) in
        (match tmp1 with
          | (Some ((interp, nbe_interp, arity, plugin))) -> (let tmp2 = (if plugin then ((("FStarC_Tactics_Native" :: []), "register_plugin"), (interp :: (nbe_interp :: []))) else ((("FStarC_Tactics_Native" :: []), "register_tactic"), (interp :: []))) in
            (match tmp2 with
              | (register, args) -> (let h = (FStarC_Extraction_ML_Syntax.with_ty FStarC_Extraction_ML_Syntax.MLTY_Top (FStarC_Extraction_ML_Syntax.MLE_Name (register))) in
                let tmp3 = (FStarC_Class_Show.fStarC_Class_Show_show__int arity) in
                let tmp4 = (tmp3, None) in
                let tmp5 = (FStarC_Extraction_ML_Syntax.MLC_Int ((match tmp4 with (custard_tup, _) -> custard_tup), (match tmp4 with (_, custard_tup) -> custard_tup))) in
                let arity1 = (FStarC_Extraction_ML_Syntax.MLE_Const (tmp5)) in
                let app = (FStarC_Extraction_ML_Syntax.with_ty FStarC_Extraction_ML_Syntax.MLTY_Top (FStarC_Extraction_ML_Syntax.MLE_App (h, (FStarC_List.op_At ((mk ml_name_str) :: ((mk arity1) :: [])) args)))) in
                ((FStarC_Extraction_ML_Syntax.mk_mlmodule1 (FStarC_Extraction_ML_Syntax.MLM_Top (app))) :: []))
            ))
          | None -> []
        ))) in
      (FStarC_List.collect mk_registration (Custard_FStar_Pervasives_Native.fStar_Pervasives_Native_snd lbs)))
    | (FStarC_Syntax_Syntax.Sig_bundle ({ FStarC_Syntax_Syntax.ses = ses; lids = tmp; _ })) -> (let mutual_sigelts = (FStarC_List.filter (fun se1 -> (match (se1).FStarC_Syntax_Syntax.sigel with
          | (FStarC_Syntax_Syntax.Sig_inductive_typ (tmp1)) -> true
          | tmp1 -> false
        )) ses) in
      let mutual_lids = (FStarC_List.map (fun se1 -> (match (se1).FStarC_Syntax_Syntax.sigel with
          | (FStarC_Syntax_Syntax.Sig_inductive_typ ({ FStarC_Syntax_Syntax.lid = lid; us = tmp1; params = tmp2; num_uniform_params = tmp3; t = tmp4; mutuals = tmp5; ds = tmp6; injective_type_params = tmp7; _ })) -> lid
        )) mutual_sigelts) in
      let proc_one = (fun typ_sigelt -> (let tmp1 = (typ_sigelt).FStarC_Syntax_Syntax.sigel in
        (match tmp1 with
          | (FStarC_Syntax_Syntax.Sig_inductive_typ ({ FStarC_Syntax_Syntax.lid = tlid; us = tmp2; params = ps; num_uniform_params = tmp3; t = tmp4; mutuals = tmp5; ds = tmp6; injective_type_params = tmp7; _ })) -> ((if (match ps with
              | (hd :: tl) -> true
              | tmp8 -> false
            ) then (raise (Unsupported ("parameters on inductive"))) else ());
            let ns = (FStarC_Ident.ns_of_lid tlid) in
            let name = (FStarC_Ident.string_of_id (FStarC_List.last (FStarC_Ident.ids_of_lid tlid))) in
            let ctors = (FStarC_List.filter (fun se1 -> (match (se1).FStarC_Syntax_Syntax.sigel with
                | (FStarC_Syntax_Syntax.Sig_datacon ({ FStarC_Syntax_Syntax.lid = tmp8; us = tmp9; t = tmp10; ty_lid = ty_lid; num_ty_params = tmp11; mutuals = tmp12; injective_type_params = tmp13; proj_disc_lids = tmp14; _ })) -> (FStarC_Ident.lid_equals ty_lid tlid)
                | tmp8 -> false
              )) ses) in
            let ml_name_ = (mk (FStarC_Extraction_ML_Syntax.MLE_Const ((FStarC_Extraction_ML_Syntax.MLC_String ((FStarC_Ident.string_of_lid tlid)))))) in
            let tmp8 = (FStarC_List.find (fun tmp8 -> (match tmp8 with
                | (FStarC_Syntax_Syntax.RecordType (u__1, u__2)) -> true
                | tmp9 -> false
              )) (typ_sigelt).FStarC_Syntax_Syntax.sigquals) in
            let record_fields = (match tmp8 with
                | (Some ((FStarC_Syntax_Syntax.RecordType (tmp9, b)))) -> (let tmp10 = (FStarC_List.map (fun f -> (FStarC_Extraction_ML_UEnv.lookup_record_field_name g (tlid, f))) b) in
                  (Some (tmp10)))
                | tmp9 -> None
              ) in
            let tcenv = (FStarC_Extraction_ML_UEnv.tcenv_of_uenv g) in
            let ml_unembed = (mk_unembed tcenv mutual_lids record_fields ctors) in
            let ml_embed = (mk_embed tcenv mutual_lids record_fields ctors) in
            let def = (mk (FStarC_Extraction_ML_Syntax.MLE_App ((mk (FStarC_Extraction_ML_Syntax.MLE_Name ((("FStarC" :: ("Syntax" :: ("Embeddings" :: ("Base" :: [])))), "mk_extracted_embedding")))), (ml_name_ :: (ml_unembed :: (ml_embed :: [])))))) in
            let def1 = (mk (FStarC_Extraction_ML_Syntax.MLE_Fun (((mk_binder "_" FStarC_Extraction_ML_Syntax.MLTY_Erased) :: []), def))) in
            let lb = { FStarC_Extraction_ML_Syntax.mllb_name = (Prims.strcat "__knot_e_" name);
                mllb_tysc = None;
                mllb_add_unit = false;
                mllb_def = def1;
                mllb_attrs = [];
                mllb_meta = [];
                print_typ = false } in
            let tmp9 = (FStarC_Ident.lid_of_ns_and_id ns (FStarC_Ident.mk_ident ((Prims.strcat "e_" name), FStarC_Range_Type.dummyRange))) in
            let tmp10 = { arity = (Prims.parse_int "0"); syn_emb = tmp9; nbe_emb = None } in
            (register_embedding tlid tmp10);
            (lb :: []))
        ))) in
      let lbs = (FStarC_List.concatMap proc_one mutual_sigelts) in
      let unthunking = (FStarC_List.concatMap (fun se1 -> (let tlid = (match (se1).FStarC_Syntax_Syntax.sigel with
            | (FStarC_Syntax_Syntax.Sig_inductive_typ ({ FStarC_Syntax_Syntax.lid = tlid; us = tmp1; params = tmp2; num_uniform_params = tmp3; t = tmp4; mutuals = tmp5; ds = tmp6; injective_type_params = tmp7; _ })) -> tlid
          ) in
        let name = (FStarC_Ident.string_of_id (FStarC_List.last (FStarC_Ident.ids_of_lid tlid))) in
        let head = (mk (FStarC_Extraction_ML_Syntax.MLE_Var ((Prims.strcat "__knot_e_" name)))) in
        let app = (mk (FStarC_Extraction_ML_Syntax.MLE_App (head, (FStarC_Extraction_ML_Syntax.ml_unit :: [])))) in
        let lb = { FStarC_Extraction_ML_Syntax.mllb_name = (Prims.strcat "e_" name);
            mllb_tysc = None;
            mllb_add_unit = false;
            mllb_def = app;
            mllb_attrs = [];
            mllb_meta = [];
            print_typ = false } in
        ((FStarC_Extraction_ML_Syntax.mk_mlmodule1 (FStarC_Extraction_ML_Syntax.MLM_Let ((FStarC_Extraction_ML_Syntax.NonRec, (lb :: []))))) :: []))) mutual_sigelts) in
      (FStarC_List.op_At ((FStarC_Extraction_ML_Syntax.mk_mlmodule1 (FStarC_Extraction_ML_Syntax.MLM_Let ((FStarC_Extraction_ML_Syntax.Rec, lbs)))) :: []) unthunking))
    | tmp -> []
  )

let do_handle_plugin (g : FStarC_Extraction_ML_UEnv.uenv) (arity_opt : (Prims.int) option) (se : FStarC_Syntax_Syntax.sigelt) : (FStarC_Extraction_ML_Syntax.mlmodule1) list =
  (try (u___do_handle_plugin g arity_opt se) with
    | (Unsupported (msg)) -> (let tmp = (FStarC_Syntax_Print.sigelt_to_string_short se) in
      let tmp1 = (FStarC_Format.fmt2 "Could not generate a plugin for %s, reason = %s" tmp msg) in
      (FStarC_Errors.fStarC_Errors_log_issue__sigelt_string se FStarC_Errors_Codes.Warning_PluginNotImplemented tmp1);
      [])
    | (NoEmbedding (msg)) -> (let tmp = (FStarC_Syntax_Print.sigelt_to_string_short se) in
      (not_implemented_warning (se).FStarC_Syntax_Syntax.sigrng tmp msg);
      [])
  )

let maybe_register_plugin (g : FStarC_Extraction_ML_UEnv.uenv) (se : FStarC_Syntax_Syntax.sigelt) : (FStarC_Extraction_ML_Syntax.mlmodule1) list =
  (let plugin_with_arity = (fun attrs -> (FStarC_Util.find_map attrs (fun t -> (let tmp = (FStarC_Syntax_Util.head_and_args_full t) in
    (match tmp with
      | (head, args) -> (let tmp1 = (FStarC_Syntax_Util.is_fvar FStarC_Parser_Const.plugin_attr head) in
        let tmp2 = (not tmp1) in
        (if tmp2 then None else (match args with
          | ((a, tmp3) :: []) -> (let nopt = (FStarC_Syntax_Embeddings_Base.unembed FStarC_Syntax_Embeddings.e_int a FStarC_Syntax_Embeddings_Base.id_norm_cb) in
            (Some (nopt)))
          | tmp3 -> (Some (None))
        )))
    ))))) in
  let tmp = (FStarC_Options.codegen ()) in
  let tmp1 = (FStarC_List.mem tmp ((Some (FStarC_Options.Plugin)) :: [])) in
  let tmp2 = (not tmp1) in
  (if tmp2 then [] else (let tmp3 = (plugin_with_arity (se).FStarC_Syntax_Syntax.sigattrs) in
  (match tmp3 with
    | None -> []
    | (Some (tmp4)) when (FStarC_List.existsb (fun tmp5 -> (match tmp5 with
      | (FStarC_Syntax_Syntax.Projector (u__1, u__2)) -> true
      | (FStarC_Syntax_Syntax.Discriminator (tmp6)) -> true
      | tmp6 -> false
    )) (se).FStarC_Syntax_Syntax.sigquals) -> []
    | (Some (arity_opt)) -> (do_handle_plugin g arity_opt se)
  ))))

