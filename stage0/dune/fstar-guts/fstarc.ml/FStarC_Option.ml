(* Generated by F* Custard extraction. Do not edit. *)
[@@@ocaml.warning "-3-5-8-11-20-26-27-28-32-33-34-35-37-39-50-57-60-69-70"]

let map (f : ('u_'a -> 'u_'b)) (o : ('u_'a) option) : ('u_'b) option =
  (match o with
    | (Some (x)) -> (let tmp = (f x) in
      (Some (tmp)))
    | None -> None
  )

let must (tmp : ('u_'a) option) : 'u_'a =
  (match tmp with
    | (Some (x)) -> x
    | None -> (FStarC_Effect.failwith "FStarC.Option.must: called on None")
  )

let bind (o : ('u_'a) option) (f : ('u_'a -> ('u_'b) option)) : ('u_'b) option =
  (match o with
    | (Some (x)) -> (f x)
    | None -> None
  )

let dflt (d : 'u_'a) (tmp : ('u_'a) option) : 'u_'a =
  (match tmp with
    | (Some (x)) -> x
    | None -> d
  )

let rec find (f : ('u_'a -> bool)) (xs : ('u_'a) list) : ('u_'a) option =
  (match xs with
    | [] -> None
    | (x :: xs1) -> (let tmp = (f x) in
      (if tmp then (Some (x)) else (find f xs1)))
  )

let catch (o : ('u_'a) option) (f : (unit -> ('u_'a) option)) : ('u_'a) option =
  (match o with
    | (Some (x)) -> (Some (x))
    | None -> (f ())
  )

let iter (f : ('u_'a -> unit)) (tmp : ('u_'a) option) : unit =
  (match tmp with
    | (Some (x)) -> (f x)
    | None -> ()
  )

