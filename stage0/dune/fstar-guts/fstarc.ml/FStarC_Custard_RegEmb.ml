(* Generated by F* Custard extraction. Do not edit. *)
[@@@ocaml.warning "-3-5-8-11-20-26-27-28-32-33-34-35-37-39-50-57-60-69-70"]

let requested (roots : (FStarC_Ident.lident) list) (md : FStarC_Syntax_Syntax.modul) : bool =
  (let m = (FStarC_Ident.string_of_lid (md).FStarC_Syntax_Syntax.name) in
  (FStarC_List.existsb (fun l -> ((=) (FStarC_Ident.string_of_lid l) m)) roots))

let plugin_arity (attrs : ((FStarC_Syntax_Syntax.term') FStarC_Syntax_Syntax.syntax) list) : ((Prims.int) option) option =
  (FStarC_Util.find_map attrs (fun t -> (let tmp = (FStarC_Syntax_Util.head_and_args_full t) in
  (match tmp with
    | (head, args) -> (let tmp1 = (FStarC_Syntax_Util.is_fvar FStarC_Parser_Const.plugin_attr head) in
      let tmp2 = (not tmp1) in
      (if tmp2 then None else (match args with
        | ((a, tmp3) :: []) -> (let tmp4 = (FStarC_Syntax_Embeddings_Base.unembed FStarC_Syntax_Embeddings.e_int a FStarC_Syntax_Embeddings_Base.id_norm_cb) in
          (Some (tmp4)))
        | tmp3 -> (Some (None))
      )))
  ))))

let plugin_norm_steps : (FStarC_TypeChecker_Env.step) list =
  (FStarC_TypeChecker_Env.EraseUniverses :: (FStarC_TypeChecker_Env.AllowUnboundUniverses :: ((FStarC_TypeChecker_Env.UnfoldUntil (FStarC_Syntax_Syntax.delta_constant)) :: (FStarC_TypeChecker_Env.ForExtraction :: []))))

exception NoEmbedding of string

let split_type_binders (bs : (FStarC_Syntax_Syntax.binder) list) : ((FStarC_Syntax_Syntax.binder) list * (FStarC_Syntax_Syntax.binder) list) =
  (let is_type = (fun b -> (let tmp = (FStarC_Syntax_Subst.compress ((b).FStarC_Syntax_Syntax.binder_bv).FStarC_Syntax_Syntax.sort) in
    let tmp1 = (tmp).FStarC_Syntax_Syntax.n in
    (match tmp1 with
      | (FStarC_Syntax_Syntax.Tm_type (u__0)) -> true
      | tmp2 -> false
    ))) in
  let tmp = (FStarC_Util.prefix_until (fun b -> (let tmp = (is_type b) in
    (not tmp))) bs) in
  (match tmp with
    | None -> (bs, [])
    | (Some ((tvs, x, rest))) -> (tvs, (x :: rest))
  ))

type kind =
  | SyntaxTerm
  | NBETerm


let lid (s : string) : FStarC_Ident.lident =
  (FStarC_Ident.lid_of_str s)

let arg_typ (k : kind) : (FStarC_Syntax_Syntax.term') FStarC_Syntax_Syntax.syntax =
  (match k with
    | SyntaxTerm -> (let tmp = (lid "FStarC.Syntax.Syntax.term") in
      (FStarC_Syntax_Syntax.fvar tmp None))
    | NBETerm -> (let tmp = (lid "FStarC.TypeChecker.NBETerm.t") in
      (FStarC_Syntax_Syntax.fvar tmp None))
  )

type embedding_data = {
  arity : Prims.int;
  syn_emb : FStarC_Ident.lident;
  nbe_emb : (FStarC_Ident.lident) option;
}

let builtin_embeddings : ((FStarC_Ident.lident * embedding_data)) list =
  (let syn = (fun s -> (FStarC_Ident.lid_of_path ("FStarC" :: ("Syntax" :: ("Embeddings" :: (s :: [])))) FStarC_Range_Type.dummyRange)) in
  let nbe = (fun s -> (FStarC_Ident.lid_of_path ("FStarC" :: ("TypeChecker" :: ("NBETerm" :: (s :: [])))) FStarC_Range_Type.dummyRange)) in
  let refl = (fun s -> (FStarC_Ident.lid_of_path ("FStarC" :: ("Reflection" :: ("V2" :: ("Embeddings" :: (s :: []))))) FStarC_Range_Type.dummyRange)) in
  let nbe_refl = (fun s -> (FStarC_Ident.lid_of_path ("FStarC" :: ("Reflection" :: ("V2" :: ("NBEEmbeddings" :: (s :: []))))) FStarC_Range_Type.dummyRange)) in
  let base = (fun n s -> (let tmp = (syn s) in
    let tmp1 = (nbe s) in
    let tmp2 = (Some (tmp1)) in
    { arity = n; syn_emb = tmp; nbe_emb = tmp2 })) in
  let rfl = (fun s -> (let tmp = (refl s) in
    let tmp1 = (nbe_refl s) in
    let tmp2 = (Some (tmp1)) in
    { arity = (Prims.parse_int "0"); syn_emb = tmp; nbe_emb = tmp2 })) in
  let tmp = (base (Prims.parse_int "0") "e_int") in
  let tmp1 = (FStarC_Parser_Const.int_lid, tmp) in
  let tmp2 = (base (Prims.parse_int "0") "e_bool") in
  let tmp3 = (FStarC_Parser_Const.bool_lid, tmp2) in
  let tmp4 = (base (Prims.parse_int "0") "e_unit") in
  let tmp5 = (FStarC_Parser_Const.unit_lid, tmp4) in
  let tmp6 = (base (Prims.parse_int "0") "e_string") in
  let tmp7 = (FStarC_Parser_Const.string_lid, tmp6) in
  let tmp8 = (base (Prims.parse_int "0") "e_norm_step") in
  let tmp9 = (FStarC_Parser_Const.norm_step_lid, tmp8) in
  let tmp10 = (base (Prims.parse_int "0") "e_range") in
  let tmp11 = (FStarC_Parser_Const.range_lid, tmp10) in
  let tmp12 = (base (Prims.parse_int "0") "e_vconfig") in
  let tmp13 = (FStarC_Parser_Const.vconfig_lid, tmp12) in
  let tmp14 = (base (Prims.parse_int "1") "e_list") in
  let tmp15 = (FStarC_Parser_Const.list_lid, tmp14) in
  let tmp16 = (base (Prims.parse_int "1") "e_option") in
  let tmp17 = (FStarC_Parser_Const.option_lid, tmp16) in
  let tmp18 = (base (Prims.parse_int "1") "e_sealed") in
  let tmp19 = (FStarC_Parser_Const.sealed_lid, tmp18) in
  let tmp20 = (FStarC_Parser_Const_Tuples.mk_tuple_lid (Prims.parse_int "2") FStarC_Range_Type.dummyRange) in
  let tmp21 = (base (Prims.parse_int "2") "e_tuple2") in
  let tmp22 = (tmp20, tmp21) in
  let tmp23 = (FStarC_Parser_Const_Tuples.mk_tuple_lid (Prims.parse_int "3") FStarC_Range_Type.dummyRange) in
  let tmp24 = (base (Prims.parse_int "3") "e_tuple3") in
  let tmp25 = (tmp23, tmp24) in
  let tmp26 = (base (Prims.parse_int "2") "e_either") in
  let tmp27 = (FStarC_Parser_Const.either_lid, tmp26) in
  let tmp28 = (FStarC_Reflection_V2_Constants.fstar_refl_types_lid "namedv") in
  let tmp29 = (rfl "e_namedv") in
  let tmp30 = (tmp28, tmp29) in
  let tmp31 = (FStarC_Reflection_V2_Constants.fstar_refl_types_lid "bv") in
  let tmp32 = (rfl "e_bv") in
  let tmp33 = (tmp31, tmp32) in
  let tmp34 = (FStarC_Reflection_V2_Constants.fstar_refl_types_lid "binder") in
  let tmp35 = (rfl "e_binder") in
  let tmp36 = (tmp34, tmp35) in
  let tmp37 = (FStarC_Reflection_V2_Constants.fstar_refl_types_lid "term") in
  let tmp38 = (rfl "e_term") in
  let tmp39 = (tmp37, tmp38) in
  let tmp40 = (FStarC_Reflection_V2_Constants.fstar_refl_types_lid "env") in
  let tmp41 = (rfl "e_env") in
  let tmp42 = (tmp40, tmp41) in
  let tmp43 = (FStarC_Reflection_V2_Constants.fstar_refl_types_lid "fv") in
  let tmp44 = (rfl "e_fv") in
  let tmp45 = (tmp43, tmp44) in
  let tmp46 = (FStarC_Reflection_V2_Constants.fstar_refl_types_lid "comp") in
  let tmp47 = (rfl "e_comp") in
  let tmp48 = (tmp46, tmp47) in
  let tmp49 = (FStarC_Reflection_V2_Constants.fstar_refl_types_lid "sigelt") in
  let tmp50 = (rfl "e_sigelt") in
  let tmp51 = (tmp49, tmp50) in
  let tmp52 = (FStarC_Reflection_V2_Constants.fstar_refl_types_lid "ctx_uvar_and_subst") in
  let tmp53 = (rfl "e_ctx_uvar_and_subst") in
  let tmp54 = (tmp52, tmp53) in
  let tmp55 = (FStarC_Reflection_V2_Constants.fstar_refl_types_lid "letbinding") in
  let tmp56 = (rfl "e_letbinding") in
  let tmp57 = (tmp55, tmp56) in
  let tmp58 = (FStarC_Reflection_V2_Constants.fstar_refl_types_lid "ident") in
  let tmp59 = (rfl "e_ident") in
  let tmp60 = (tmp58, tmp59) in
  let tmp61 = (FStarC_Reflection_V2_Constants.fstar_refl_types_lid "universe_uvar") in
  let tmp62 = (rfl "e_universe_uvar") in
  let tmp63 = (tmp61, tmp62) in
  let tmp64 = (FStarC_Reflection_V2_Constants.fstar_refl_types_lid "universe") in
  let tmp65 = (rfl "e_universe") in
  let tmp66 = (tmp64, tmp65) in
  let tmp67 = (FStarC_Reflection_V2_Constants.fstar_refl_data_lid "vconst") in
  let tmp68 = (rfl "e_vconst") in
  let tmp69 = (tmp67, tmp68) in
  let tmp70 = (FStarC_Reflection_V2_Constants.fstar_refl_data_lid "aqualv") in
  let tmp71 = (rfl "e_aqualv") in
  let tmp72 = (tmp70, tmp71) in
  let tmp73 = (FStarC_Reflection_V2_Constants.fstar_refl_data_lid "pattern") in
  let tmp74 = (rfl "e_pattern") in
  let tmp75 = (tmp73, tmp74) in
  let tmp76 = (FStarC_Reflection_V2_Constants.fstar_refl_data_lid "namedv_view") in
  let tmp77 = (rfl "e_namedv_view") in
  let tmp78 = (tmp76, tmp77) in
  let tmp79 = (FStarC_Reflection_V2_Constants.fstar_refl_data_lid "bv_view") in
  let tmp80 = (rfl "e_bv_view") in
  let tmp81 = (tmp79, tmp80) in
  let tmp82 = (FStarC_Reflection_V2_Constants.fstar_refl_data_lid "binder_view") in
  let tmp83 = (rfl "e_binder_view") in
  let tmp84 = (tmp82, tmp83) in
  let tmp85 = (FStarC_Reflection_V2_Constants.fstar_refl_data_lid "binding") in
  let tmp86 = (rfl "e_binding") in
  let tmp87 = (tmp85, tmp86) in
  let tmp88 = (FStarC_Reflection_V2_Constants.fstar_refl_data_lid "universe_view") in
  let tmp89 = (rfl "e_universe_view") in
  let tmp90 = (tmp88, tmp89) in
  let tmp91 = (FStarC_Reflection_V2_Constants.fstar_refl_data_lid "term_view") in
  let tmp92 = (rfl "e_term_view") in
  let tmp93 = (tmp91, tmp92) in
  let tmp94 = (FStarC_Reflection_V2_Constants.fstar_refl_data_lid "comp_view") in
  let tmp95 = (rfl "e_comp_view") in
  let tmp96 = (tmp94, tmp95) in
  let tmp97 = (FStarC_Reflection_V2_Constants.fstar_refl_data_lid "lb_view") in
  let tmp98 = (rfl "e_lb_view") in
  let tmp99 = (tmp97, tmp98) in
  let tmp100 = (FStarC_Reflection_V2_Constants.fstar_refl_data_lid "sigelt_view") in
  let tmp101 = (rfl "e_sigelt_view") in
  let tmp102 = (tmp100, tmp101) in
  let tmp103 = (FStarC_Reflection_V2_Constants.fstar_refl_data_lid "qualifier") in
  let tmp104 = (rfl "e_qualifier") in
  let tmp105 = (tmp103, tmp104) in
  let tmp106 = (tmp105 :: []) in
  let tmp107 = (tmp102 :: tmp106) in
  let tmp108 = (tmp99 :: tmp107) in
  let tmp109 = (tmp96 :: tmp108) in
  let tmp110 = (tmp93 :: tmp109) in
  let tmp111 = (tmp90 :: tmp110) in
  let tmp112 = (tmp87 :: tmp111) in
  let tmp113 = (tmp84 :: tmp112) in
  let tmp114 = (tmp81 :: tmp113) in
  let tmp115 = (tmp78 :: tmp114) in
  let tmp116 = (tmp75 :: tmp115) in
  let tmp117 = (tmp72 :: tmp116) in
  let tmp118 = (tmp69 :: tmp117) in
  let tmp119 = (tmp66 :: tmp118) in
  let tmp120 = (tmp63 :: tmp119) in
  let tmp121 = (tmp60 :: tmp120) in
  let tmp122 = (tmp57 :: tmp121) in
  let tmp123 = (tmp54 :: tmp122) in
  let tmp124 = (tmp51 :: tmp123) in
  let tmp125 = (tmp48 :: tmp124) in
  let tmp126 = (tmp45 :: tmp125) in
  let tmp127 = (tmp42 :: tmp126) in
  let tmp128 = (tmp39 :: tmp127) in
  let tmp129 = (tmp36 :: tmp128) in
  let tmp130 = (tmp33 :: tmp129) in
  let tmp131 = (tmp30 :: tmp130) in
  let tmp132 = (tmp27 :: tmp131) in
  let tmp133 = (tmp25 :: tmp132) in
  let tmp134 = (tmp22 :: tmp133) in
  let tmp135 = (tmp19 :: tmp134) in
  let tmp136 = (tmp17 :: tmp135) in
  let tmp137 = (tmp15 :: tmp136) in
  let tmp138 = (tmp13 :: tmp137) in
  let tmp139 = (tmp11 :: tmp138) in
  let tmp140 = (tmp9 :: tmp139) in
  let tmp141 = (tmp7 :: tmp140) in
  let tmp142 = (tmp5 :: tmp141) in
  let tmp143 = (tmp3 :: tmp142) in
  (tmp1 :: tmp143))

let find_embedding (l : FStarC_Ident.lident) : (embedding_data) option =
  (let tmp = (FStarC_List.find (fun tmp -> (match tmp with
      | (l', tmp1) -> (FStarC_Ident.lid_equals l l')
    )) builtin_embeddings) in
  (match tmp with
    | (Some ((tmp1, d))) -> (Some (d))
    | None -> None
  ))

let rec whnf__go (st : FStarC_Custard_Extract.state) (head_lid : ((FStarC_Syntax_Syntax.term') FStarC_Syntax_Syntax.syntax -> (FStarC_Ident.lident) option)) (fuel : Prims.int) (t : (FStarC_Syntax_Syntax.term') FStarC_Syntax_Syntax.syntax) : (FStarC_Syntax_Syntax.term') FStarC_Syntax_Syntax.syntax =
  (let tmp = (head_lid t) in
  (match tmp with
    | None -> t
    | (Some (l)) when (let tmp1 = (find_embedding l) in
    (match tmp1 with
      | (Some (v)) -> true
      | tmp2 -> false
    )) -> t
    | (Some (l)) -> ((FStarC_Custard_Extract.ensure_lid_available st l);
      let tmp1 = (FStarC_Custard_Extract.tcenv st) in
      let t' = (FStarC_Custard_Mono.norm_bounded tmp1 "an embedded type" (FStarC_TypeChecker_Env.Primops :: (FStarC_TypeChecker_Env.Weak :: (FStarC_TypeChecker_Env.HNF :: ((FStarC_TypeChecker_Env.UnfoldUntil (FStarC_Syntax_Syntax.delta_constant)) :: (FStarC_TypeChecker_Env.Beta :: []))))) t) in
      let tmp2 = (FStarC_Syntax_Util.un_uinst t') in
      let t'1 = (FStarC_Syntax_Subst.compress tmp2) in
      (if (Prims.op_Less_Equals fuel (Prims.parse_int "0")) then t'1 else (let tmp3 = (head_lid t'1) in
      (match tmp3 with
        | (Some (l')) when (FStarC_Ident.lid_equals l l') -> t'1
        | tmp4 -> ((whnf__go st head_lid) (Prims.op_Minus fuel (Prims.parse_int "1")) t'1)
      ))))
  ))

let whnf (st : FStarC_Custard_Extract.state) (t : (FStarC_Syntax_Syntax.term') FStarC_Syntax_Syntax.syntax) : (FStarC_Syntax_Syntax.term') FStarC_Syntax_Syntax.syntax =
  (let head_lid = (fun t1 -> (let tmp = (FStarC_Syntax_Util.head_and_args_full t1) in
    let tmp1 = (Custard_FStar_Pervasives_Native.fStar_Pervasives_Native_fst tmp) in
    let tmp2 = (FStarC_Syntax_Util.un_uinst tmp1) in
    let tmp3 = (FStarC_Syntax_Subst.compress tmp2) in
    let tmp4 = (tmp3).FStarC_Syntax_Syntax.n in
    (match tmp4 with
      | (FStarC_Syntax_Syntax.Tm_fvar (fv)) -> (Some ((FStarC_Syntax_Syntax.lid_of_fv fv)))
      | tmp5 -> None
    ))) in
  let tmp = (FStarC_Syntax_Util.un_uinst t) in
  let tmp1 = (FStarC_Syntax_Subst.compress tmp) in
  ((whnf__go st head_lid) (Prims.parse_int "20") tmp1))

let find_tv (env : ((FStarC_Syntax_Syntax.bv * FStarC_Syntax_Syntax.bv)) list) (b : FStarC_Syntax_Syntax.bv) : (FStarC_Syntax_Syntax.bv) option =
  (let tmp = (FStarC_List.tryFind (fun tmp -> (match tmp with
      | (a, tmp1) -> (FStarC_Syntax_Syntax.bv_eq a b)
    )) env) in
  (match tmp with
    | (Some ((tmp1, v))) -> (Some (v))
    | None -> None
  ))

let call (f : FStarC_Ident.lident) (tys : ((FStarC_Syntax_Syntax.term') FStarC_Syntax_Syntax.syntax) list) (args : ((FStarC_Syntax_Syntax.term') FStarC_Syntax_Syntax.syntax) list) : (FStarC_Syntax_Syntax.term') FStarC_Syntax_Syntax.syntax =
  (let tmp = (FStarC_Syntax_Syntax.fvar f None) in
  let tmp1 = (FStarC_List.map FStarC_Syntax_Syntax.iarg tys) in
  let tmp2 = (FStarC_List.map FStarC_Syntax_Syntax.as_arg args) in
  let tmp3 = (FStarC_List.op_At tmp1 tmp2) in
  (FStarC_Syntax_Util.mk_app tmp tmp3))

let embedding_head (st : FStarC_Custard_Extract.state) (k : kind) (t : (FStarC_Syntax_Syntax.term') FStarC_Syntax_Syntax.syntax) : (FStarC_Ident.lident) option =
  (let tmp = (FStarC_Syntax_Util.un_uinst t) in
  let tmp1 = (FStarC_Syntax_Subst.compress tmp) in
  let tmp2 = (tmp1).FStarC_Syntax_Syntax.n in
  (match tmp2 with
    | (FStarC_Syntax_Syntax.Tm_fvar (fv)) -> (let l = (FStarC_Syntax_Syntax.lid_of_fv fv) in
      let tmp3 = (find_embedding l) in
      (match tmp3 with
        | (Some (d)) -> (match k with
            | SyntaxTerm -> (Some ((d).syn_emb))
            | NBETerm -> (d).nbe_emb
          )
        | None -> None
      ))
    | tmp3 -> None
  ))

let nbe_unsupported (t : (FStarC_Syntax_Syntax.term') FStarC_Syntax_Syntax.syntax) : (FStarC_Syntax_Syntax.term') FStarC_Syntax_Syntax.syntax =
  (let tmp = (lid "FStarC.TypeChecker.NBETerm.e_unsupported") in
  (call tmp (t :: []) []))

type gen_emb = {
  ge_lid : FStarC_Ident.lident;
  ge_ph : FStarC_Syntax_Syntax.bv;
  ge_emb : FStarC_Custard_Syntax.name;
  ge_knot : FStarC_Custard_Syntax.name;
}

let generated : ((gen_emb) list ref) =
  (ref [])

let find_generated (l : FStarC_Ident.lident) : (gen_emb) option =
  (let tmp = (!(generated)) in
  (FStarC_List.find (fun g -> (FStarC_Ident.lid_equals (g).ge_lid l)) tmp))

let embedding_typ (t : (FStarC_Syntax_Syntax.term') FStarC_Syntax_Syntax.syntax) : (FStarC_Syntax_Syntax.term') FStarC_Syntax_Syntax.syntax =
  (let tmp = (FStarC_Ident.lid_of_str "FStarC.Syntax.Embeddings.Base.embedding") in
  let tmp1 = (FStarC_Syntax_Syntax.fvar tmp None) in
  (FStarC_Syntax_Util.mk_app tmp1 ((FStarC_Syntax_Syntax.as_arg t) :: [])))

let building : ((FStarC_Ident.lident) list ref) =
  (ref [])

let emb_base (s : string) : FStarC_Ident.lident =
  (FStarC_Ident.lid_of_str (Prims.strcat "FStarC.Syntax.Embeddings.Base." s))

let str (s : string) : (FStarC_Syntax_Syntax.term') FStarC_Syntax_Syntax.syntax =
  (FStarC_Syntax_Syntax.mk (FStarC_Syntax_Syntax.Tm_constant ((FStarC_Const.Const_string (s, FStarC_Range_Type.dummyRange)))) FStarC_Range_Type.dummyRange)

let dummy : FStarC_Range_Type.range =
  FStarC_Range_Type.dummyRange

let ctor_fv (l : FStarC_Ident.lident) : FStarC_Syntax_Syntax.fv =
  (FStarC_Syntax_Syntax.lid_and_dd_as_fv l (Some (FStarC_Syntax_Syntax.Data_ctor)))

let pat_of (v : FStarC_Syntax_Syntax.pat') : (FStarC_Syntax_Syntax.pat') FStarC_Syntax_Syntax.withinfo_t =
  (FStarC_Syntax_Syntax.withinfo v dummy)

let dot_pat : (FStarC_Syntax_Syntax.pat') FStarC_Syntax_Syntax.withinfo_t =
  (pat_of (FStarC_Syntax_Syntax.Pat_dot_term (None)))

let rec list_pat (vs : (FStarC_Syntax_Syntax.bv) list) : (FStarC_Syntax_Syntax.pat') FStarC_Syntax_Syntax.withinfo_t =
  (match vs with
    | [] -> (let tmp = (ctor_fv FStarC_Parser_Const.nil_lid) in
      let tmp1 = (tmp, None, ((dot_pat, true) :: [])) in
      let tmp2 = (FStarC_Syntax_Syntax.Pat_cons ((match tmp1 with (custard_tup, _, _) -> custard_tup), (match tmp1 with (_, custard_tup, _) -> custard_tup), (match tmp1 with (_, _, custard_tup) -> custard_tup))) in
      (pat_of tmp2))
    | (v :: vs1) -> (let tmp = (ctor_fv FStarC_Parser_Const.cons_lid) in
      let tmp1 = (list_pat vs1) in
      let tmp2 = (tmp1, false) in
      let tmp3 = (tmp2 :: []) in
      let tmp4 = (((pat_of (FStarC_Syntax_Syntax.Pat_var (v))), false) :: tmp3) in
      let tmp5 = ((dot_pat, true) :: tmp4) in
      let tmp6 = (tmp, None, tmp5) in
      let tmp7 = (FStarC_Syntax_Syntax.Pat_cons ((match tmp6 with (custard_tup, _, _) -> custard_tup), (match tmp6 with (_, custard_tup, _) -> custard_tup), (match tmp6 with (_, _, custard_tup) -> custard_tup))) in
      (pat_of tmp7))
  )

let rec term_list (ty : (FStarC_Syntax_Syntax.term') FStarC_Syntax_Syntax.syntax) (es : ((FStarC_Syntax_Syntax.term') FStarC_Syntax_Syntax.syntax) list) : (FStarC_Syntax_Syntax.term') FStarC_Syntax_Syntax.syntax =
  (match es with
    | [] -> (let tmp = (FStarC_Syntax_Syntax.tdataconstr FStarC_Parser_Const.nil_lid) in
      (FStarC_Syntax_Util.mk_app tmp ((FStarC_Syntax_Syntax.iarg ty) :: [])))
    | (e :: es1) -> (let tmp = (FStarC_Syntax_Syntax.tdataconstr FStarC_Parser_Const.cons_lid) in
      let tmp1 = (term_list ty es1) in
      let tmp2 = (FStarC_Syntax_Syntax.as_arg tmp1) in
      let tmp3 = (tmp2 :: []) in
      let tmp4 = ((FStarC_Syntax_Syntax.as_arg e) :: tmp3) in
      let tmp5 = ((FStarC_Syntax_Syntax.iarg ty) :: tmp4) in
      (FStarC_Syntax_Util.mk_app tmp tmp5))
  )

let rec subst_expr (x : string) (v : FStarC_Custard_Syntax.expr) (e : FStarC_Custard_Syntax.expr) : FStarC_Custard_Syntax.expr =
  (let go = (subst_expr x v) in
  (match (e).FStarC_Custard_Syntax.e with
    | (FStarC_Custard_Syntax.EVar (y)) when ((=) y x) -> v
    | (FStarC_Custard_Syntax.ELet (y, t, e1, e2)) -> (let tmp = (go e1) in
      let tmp1 = (if ((=) y x) then e2 else (go e2)) in
      let tmp2 = (y, t, tmp, tmp1) in
      let tmp3 = (FStarC_Custard_Syntax.ELet ((match tmp2 with (custard_tup, _, _, _) -> custard_tup), (match tmp2 with (_, custard_tup, _, _) -> custard_tup), (match tmp2 with (_, _, custard_tup, _) -> custard_tup), (match tmp2 with (_, _, _, custard_tup) -> custard_tup))) in
      { FStarC_Custard_Syntax.e = tmp3;
        ty = (e).FStarC_Custard_Syntax.ty;
        eff = (e).FStarC_Custard_Syntax.eff })
    | (FStarC_Custard_Syntax.EFun (bs, b)) -> (let shadows = (FStarC_List.existsb (fun b1 -> ((=) (b1).FStarC_Custard_Syntax.b_name x)) bs) in
      let tmp = (if shadows then b else (go b)) in
      let tmp1 = (bs, tmp) in
      let tmp2 = (FStarC_Custard_Syntax.EFun ((match tmp1 with (custard_tup, _) -> custard_tup), (match tmp1 with (_, custard_tup) -> custard_tup))) in
      { FStarC_Custard_Syntax.e = tmp2;
        ty = (e).FStarC_Custard_Syntax.ty;
        eff = (e).FStarC_Custard_Syntax.eff })
    | tmp -> (FStarC_Custard_Syntax.map_children go e)
  ))

let compile (st : FStarC_Custard_Extract.state) (t : (FStarC_Syntax_Syntax.term') FStarC_Syntax_Syntax.syntax) : FStarC_Custard_Syntax.expr =
  (let free = (FStarC_Syntax_Free.names t) in
  let tmp = (!(generated)) in
  let used = (FStarC_List.filter (fun g -> (FStarC_Syntax_Syntax.fStarC_Class_Setlike_mem__bv_list_bv (g).ge_ph free)) tmp) in
  (match used with
    | [] -> (FStarC_Custard_Extract.expr_of_term st t)
    | tmp1 -> (let e = (FStarC_Custard_Extract.expr_of_term st t) in
      (FStarC_List.fold_left (fun e1 g -> (let ety = (FStarC_Custard_Extract.ty_of_typ st ((g).ge_ph).FStarC_Syntax_Syntax.sort) in
      let tmp2 = (!(building)) in
      let tmp3 = (FStarC_List.existsb (FStarC_Ident.lid_equals (g).ge_lid) tmp2) in
      let v = (if tmp3 then (FStarC_Custard_Syntax.mk (FStarC_Custard_Syntax.EApp ((FStarC_Custard_Syntax.mk (FStarC_Custard_Syntax.EQual ((g).ge_knot, [])) (FStarC_Custard_Syntax.TArrow (FStarC_Custard_Syntax.TUnit, FStarC_Custard_Syntax.E_Impure, ety)) FStarC_Custard_Syntax.E_Pure), ((FStarC_Custard_Syntax.mk (FStarC_Custard_Syntax.EConst (FStarC_Custard_Syntax.CUnit)) FStarC_Custard_Syntax.TUnit FStarC_Custard_Syntax.E_Pure) :: []))) ety FStarC_Custard_Syntax.E_Impure) else (FStarC_Custard_Syntax.mk (FStarC_Custard_Syntax.EQual ((g).ge_emb, [])) ety FStarC_Custard_Syntax.E_Impure)) in
      let tmp4 = (FStarC_Custard_Syntax.uniq (FStarC_Ident.string_of_id ((g).ge_ph).FStarC_Syntax_Syntax.ppname) ((g).ge_ph).FStarC_Syntax_Syntax.index) in
      (subst_expr tmp4 v e1))) e used))
  ))

let rec unembed_fun (st : FStarC_Custard_Extract.state) (gty : (FStarC_Syntax_Syntax.term') FStarC_Syntax_Syntax.syntax) (ctors : ((FStarC_Ident.lident * (FStarC_Syntax_Syntax.binder) list)) list) : (FStarC_Syntax_Syntax.term') FStarC_Syntax_Syntax.syntax =
  (let tmp = (FStarC_Ident.lid_of_str "FStarC.Syntax.Syntax.term") in
  let t_term = (FStarC_Syntax_Syntax.fvar tmp None) in
  let tmp1 = (FStarC_Syntax_Syntax.fvar FStarC_Parser_Const.list_lid None) in
  let t_terms = (FStarC_Syntax_Util.mk_app tmp1 ((FStarC_Syntax_Syntax.as_arg t_term) :: [])) in
  let tmp2 = (FStarC_Parser_Const_Tuples.mk_tuple_lid (Prims.parse_int "2") dummy) in
  let tmp3 = (FStarC_Syntax_Syntax.fvar tmp2 None) in
  let scrut_ty = (FStarC_Syntax_Util.mk_app tmp3 ((FStarC_Syntax_Syntax.as_arg FStarC_Syntax_Syntax.t_string) :: ((FStarC_Syntax_Syntax.as_arg t_terms) :: []))) in
  let tm = (FStarC_Syntax_Syntax.new_bv None scrut_ty) in
  let tmp4 = (FStarC_Syntax_Syntax.tdataconstr FStarC_Parser_Const.none_lid) in
  let none = (FStarC_Syntax_Util.mk_app tmp4 ((FStarC_Syntax_Syntax.iarg gty) :: [])) in
  let brs = (FStarC_List.map (fun tmp5 -> (match tmp5 with
      | (c, bs) -> (let ps = (FStarC_List.map (fun tmp6 -> (FStarC_Syntax_Syntax.new_bv None t_term)) bs) in
        let vs = (FStarC_List.map (fun b -> (FStarC_Syntax_Syntax.new_bv None ((b).FStarC_Syntax_Syntax.binder_bv).FStarC_Syntax_Syntax.sort)) bs) in
        let tmp6 = (FStarC_Ident.lid_of_str "FStar.Pervasives.Native.Mktuple2") in
        let tmp7 = (ctor_fv tmp6) in
        let tmp8 = (list_pat ps) in
        let tmp9 = (tmp8, false) in
        let tmp10 = (tmp9 :: []) in
        let tmp11 = (((pat_of (FStarC_Syntax_Syntax.Pat_constant ((FStarC_Const.Const_string ((FStarC_Ident.string_of_lid c), dummy))))), false) :: tmp10) in
        let tmp12 = ((dot_pat, true) :: tmp11) in
        let tmp13 = ((dot_pat, true) :: tmp12) in
        let tmp14 = (tmp7, None, tmp13) in
        let tmp15 = (FStarC_Syntax_Syntax.Pat_cons ((match tmp14 with (custard_tup, _, _) -> custard_tup), (match tmp14 with (_, custard_tup, _) -> custard_tup), (match tmp14 with (_, _, custard_tup) -> custard_tup))) in
        let p = (pat_of tmp15) in
        let tmp16 = (FStarC_Syntax_Syntax.tdataconstr FStarC_Parser_Const.some_lid) in
        let tmp17 = (FStarC_Syntax_Syntax.tdataconstr c) in
        let tmp18 = (FStarC_List.map2 (fun b v -> (let tmp18 = (FStarC_Syntax_Syntax.bv_to_name v) in
          (tmp18, (FStarC_Syntax_Util.aqual_of_binder b)))) bs vs) in
        let tmp19 = (FStarC_Syntax_Util.mk_app tmp17 tmp18) in
        let tmp20 = (FStarC_Syntax_Syntax.as_arg tmp19) in
        let tmp21 = (tmp20 :: []) in
        let tmp22 = ((FStarC_Syntax_Syntax.iarg gty) :: tmp21) in
        let ret = (FStarC_Syntax_Util.mk_app tmp16 tmp22) in
        let tmp23 = (FStarC_List.map2 (fun a b -> (a, b)) ps vs) in
        let steps = (FStarC_List.map2 (fun b tmp24 -> (match tmp24 with
            | (pv, v) -> (b, pv, v)
          )) bs tmp23) in
        let body = (FStarC_List.fold_right (fun tmp24 acc -> (match tmp24 with
            | (b, pv, v) -> (let tmp25 = (FStarC_Ident.lid_of_str "FStarC.Option.bind") in
              let tmp26 = (emb_base "extracted_unembed") in
              let tmp27 = (embedding_for st SyntaxTerm [] ((b).FStarC_Syntax_Syntax.binder_bv).FStarC_Syntax_Syntax.sort) in
              let tmp28 = (FStarC_Syntax_Syntax.bv_to_name pv) in
              let tmp29 = (tmp28 :: []) in
              let tmp30 = (tmp27 :: tmp29) in
              let tmp31 = (call tmp26 (((b).FStarC_Syntax_Syntax.binder_bv).FStarC_Syntax_Syntax.sort :: []) tmp30) in
              let tmp32 = (FStarC_Syntax_Util.abs ((FStarC_Syntax_Syntax.mk_binder v) :: []) acc None) in
              let tmp33 = (tmp32 :: []) in
              let tmp34 = (tmp31 :: tmp33) in
              (call tmp25 (((b).FStarC_Syntax_Syntax.binder_bv).FStarC_Syntax_Syntax.sort :: (gty :: [])) tmp34))
          )) steps ret) in
        (FStarC_Syntax_Subst.close_branch (p, None, body)))
    )) ctors) in
  let tmp5 = (FStarC_Syntax_Syntax.new_bv None scrut_ty) in
  let tmp6 = (FStarC_Syntax_Syntax.Pat_var (tmp5)) in
  let tmp7 = (pat_of tmp6) in
  let tmp8 = (tmp7, None, none) in
  let catchall = (FStarC_Syntax_Subst.close_branch tmp8) in
  let tmp9 = (FStarC_Syntax_Syntax.bv_to_name tm) in
  let tmp10 = ({ FStarC_Syntax_Syntax.scrutinee = tmp9;
      ret_opt = None;
      brs = (FStarC_List.op_At brs (catchall :: []));
      rc_opt = None } : FStarC_Syntax_Syntax.term'__Tm_match__payload) in
  let tmp11 = (FStarC_Syntax_Syntax.Tm_match (tmp10)) in
  let tmp12 = (FStarC_Syntax_Syntax.mk tmp11 dummy) in
  (FStarC_Syntax_Util.abs ((FStarC_Syntax_Syntax.mk_binder tm) :: []) tmp12 None))

and embed_fun (st : FStarC_Custard_Extract.state) (gty : (FStarC_Syntax_Syntax.term') FStarC_Syntax_Syntax.syntax) (ctors : ((FStarC_Ident.lident * (FStarC_Syntax_Syntax.binder) list)) list) : (FStarC_Syntax_Syntax.term') FStarC_Syntax_Syntax.syntax =
  (let x = (FStarC_Syntax_Syntax.new_bv None gty) in
  let tmp = (FStarC_Ident.lid_of_str "FStarC.Syntax.Syntax.arg") in
  let arg_ty = (FStarC_Syntax_Syntax.fvar tmp None) in
  let brs = (FStarC_List.map (fun tmp1 -> (match tmp1 with
      | (c, bs) -> (let vs = (FStarC_List.map (fun b -> (FStarC_Syntax_Syntax.new_bv None ((b).FStarC_Syntax_Syntax.binder_bv).FStarC_Syntax_Syntax.sort)) bs) in
        let tmp2 = (ctor_fv c) in
        let tmp3 = (FStarC_List.map2 (fun b v -> ((pat_of (FStarC_Syntax_Syntax.Pat_var (v))), (match (b).FStarC_Syntax_Syntax.binder_qual with
            | (Some (v1)) -> true
            | tmp3 -> false
          ))) bs vs) in
        let tmp4 = (tmp2, None, tmp3) in
        let tmp5 = (FStarC_Syntax_Syntax.Pat_cons ((match tmp4 with (custard_tup, _, _) -> custard_tup), (match tmp4 with (_, custard_tup, _) -> custard_tup), (match tmp4 with (_, _, custard_tup) -> custard_tup))) in
        let p = (pat_of tmp5) in
        let args = (FStarC_List.map2 (fun b v -> (let tmp6 = (FStarC_Ident.lid_of_str "FStarC.Syntax.Syntax.as_arg") in
          let tmp7 = (emb_base "extracted_embed") in
          let tmp8 = (embedding_for st SyntaxTerm [] ((b).FStarC_Syntax_Syntax.binder_bv).FStarC_Syntax_Syntax.sort) in
          let tmp9 = (FStarC_Syntax_Syntax.bv_to_name v) in
          let tmp10 = (tmp9 :: []) in
          let tmp11 = (tmp8 :: tmp10) in
          let tmp12 = (call tmp7 (((b).FStarC_Syntax_Syntax.binder_bv).FStarC_Syntax_Syntax.sort :: []) tmp11) in
          let tmp13 = (tmp12 :: []) in
          (call tmp6 [] tmp13))) bs vs) in
        let tmp6 = (FStarC_Ident.lid_of_str "FStarC.Syntax.Syntax.tdataconstr") in
        let tmp7 = (FStarC_Ident.lid_of_str "FStarC.Ident.lid_of_str") in
        let tmp8 = (str (FStarC_Ident.string_of_lid c)) in
        let tmp9 = (tmp8 :: []) in
        let tmp10 = (call tmp7 [] tmp9) in
        let tmp11 = (tmp10 :: []) in
        let head = (call tmp6 [] tmp11) in
        let tmp12 = (FStarC_Ident.lid_of_str "FStarC.Syntax.Util.mk_app") in
        let tmp13 = (term_list arg_ty args) in
        let tmp14 = (tmp13 :: []) in
        let tmp15 = (head :: tmp14) in
        let tmp16 = (call tmp12 [] tmp15) in
        let tmp17 = (p, None, tmp16) in
        (FStarC_Syntax_Subst.close_branch tmp17))
    )) ctors) in
  let tmp1 = (FStarC_Syntax_Syntax.bv_to_name x) in
  let tmp2 = ({ FStarC_Syntax_Syntax.scrutinee = tmp1;
      ret_opt = None;
      brs = brs;
      rc_opt = None } : FStarC_Syntax_Syntax.term'__Tm_match__payload) in
  let tmp3 = (FStarC_Syntax_Syntax.Tm_match (tmp2)) in
  let tmp4 = (FStarC_Syntax_Syntax.mk tmp3 dummy) in
  (FStarC_Syntax_Util.abs ((FStarC_Syntax_Syntax.mk_binder x) :: []) tmp4 None))

and generate_one (st : FStarC_Custard_Extract.state) (g : gen_emb) : unit =
  (let env = (FStarC_Custard_Extract.tcenv st) in
  let gty = (FStarC_Syntax_Syntax.fvar (g).ge_lid None) in
  let tmp = (FStarC_TypeChecker_Env.datacons_of_typ env (g).ge_lid) in
  (match tmp with
    | (tmp1, cs) -> (let ctors = (FStarC_List.map (fun c -> (let tmp2 = (FStarC_TypeChecker_Env.lookup_sigelt env c) in
        (match tmp2 with
          | (Some ({ FStarC_Syntax_Syntax.sigel = (FStarC_Syntax_Syntax.Sig_datacon ({ FStarC_Syntax_Syntax.lid = tmp3; us = tmp4; t = t; ty_lid = tmp5; num_ty_params = tmp6; mutuals = tmp7; injective_type_params = tmp8; proj_disc_lids = tmp9; _ })); sigrng = tmp10; sigquals = tmp11; sigmeta = tmp12; sigattrs = tmp13; sigopens_and_abbrevs = tmp14; sigopts = tmp15; _ })) -> (let tmp16 = (FStarC_Syntax_Util.arrow_formals t) in
            let tmp17 = (Custard_FStar_Pervasives_Native.fStar_Pervasives_Native_fst tmp16) in
            (c, tmp17))
          | tmp3 -> (raise (NoEmbedding ((Prims.strcat "no declaration for constructor " (FStarC_Ident.string_of_lid c)))))
        ))) cs) in
      let tmp2 = (emb_base "mk_extracted_embedding") in
      let tmp3 = (str (FStarC_Ident.string_of_lid (g).ge_lid)) in
      let tmp4 = (unembed_fun st gty ctors) in
      let tmp5 = (embed_fun st gty ctors) in
      let tmp6 = (tmp5 :: []) in
      let tmp7 = (tmp4 :: tmp6) in
      let tmp8 = (tmp3 :: tmp7) in
      let tmp9 = (call tmp2 (gty :: []) tmp8) in
      let body = (compile st tmp9) in
      let tmp10 = (embedding_typ gty) in
      let ety = (FStarC_Custard_Extract.ty_of_typ st tmp10) in
      (FStarC_Custard_Extract.emit st (Prims.strcat "regemb-knot:" (FStarC_Ident.string_of_lid (g).ge_lid)) (FStarC_Custard_Syntax.DLet ({ FStarC_Custard_Syntax.dl_name = (g).ge_knot;
        dl_typars = [];
        dl_binders = ({ FStarC_Custard_Syntax.b_name = "_thunk";
          b_ty = FStarC_Custard_Syntax.TUnit } :: []);
        dl_ret = ety;
        dl_eff = FStarC_Custard_Syntax.E_Impure;
        dl_body = body;
        dl_flags = ((FStarC_Custard_Syntax.Comment ((Prims.strcat "Embedding for " (FStarC_Ident.string_of_lid (g).ge_lid)))) :: []) })));
      (FStarC_Custard_Extract.emit st (Prims.strcat "regemb-emb:" (FStarC_Ident.string_of_lid (g).ge_lid)) (FStarC_Custard_Syntax.DLet ({ FStarC_Custard_Syntax.dl_name = (g).ge_emb;
        dl_typars = [];
        dl_binders = [];
        dl_ret = ety;
        dl_eff = FStarC_Custard_Syntax.E_Impure;
        dl_body = (FStarC_Custard_Syntax.mk (FStarC_Custard_Syntax.EApp ((FStarC_Custard_Syntax.mk (FStarC_Custard_Syntax.EQual ((g).ge_knot, [])) (FStarC_Custard_Syntax.TArrow (FStarC_Custard_Syntax.TUnit, FStarC_Custard_Syntax.E_Impure, ety)) FStarC_Custard_Syntax.E_Pure), ((FStarC_Custard_Syntax.mk (FStarC_Custard_Syntax.EConst (FStarC_Custard_Syntax.CUnit)) FStarC_Custard_Syntax.TUnit FStarC_Custard_Syntax.E_Pure) :: []))) ety FStarC_Custard_Syntax.E_Impure);
        dl_flags = [] }))))
  ))

and ensure_generated (st : FStarC_Custard_Extract.state) (l : FStarC_Ident.lident) : unit =
  (let tmp = (find_generated l) in
  let tmp1 = (match tmp with
      | (Some (v)) -> true
      | tmp1 -> false
    ) in
  (if tmp1 then () else (let tmp2 = (FStarC_Custard_Extract.tcenv st) in
  let tmp3 = (FStarC_TypeChecker_Env.lookup_sigelt tmp2 l) in
  (match tmp3 with
    | (Some ({ FStarC_Syntax_Syntax.sigel = (FStarC_Syntax_Syntax.Sig_inductive_typ ({ FStarC_Syntax_Syntax.lid = tmp4; us = tmp5; params = params; num_uniform_params = tmp6; t = tmp7; mutuals = mutuals; ds = tmp8; injective_type_params = tmp9; _ })); sigrng = tmp10; sigquals = tmp11; sigmeta = tmp12; sigattrs = tmp13; sigopens_and_abbrevs = tmp14; sigopts = tmp15; _ })) -> ((if (match params with
        | (hd :: tl) -> true
        | tmp16 -> false
      ) then (raise (NoEmbedding ((Prims.strcat "cannot generate an embedding for " (Prims.strcat (FStarC_Ident.string_of_lid l) ": the inductive has parameters"))))) else ());
      let tmp16 = (FStarC_List.filter (fun m -> (not (FStarC_Ident.lid_equals m l))) mutuals) in
      let group = (l :: tmp16) in
      let entries = (FStarC_List.map (fun g -> (let nm = (FStarC_Custard_Extract.name_of_lid g) in
        let tmp17 = (FStarC_Syntax_Syntax.fvar g None) in
        let tmp18 = (embedding_typ tmp17) in
        let tmp19 = (FStarC_Syntax_Syntax.new_bv None tmp18) in
        let e = { ge_lid = g;
            ge_ph = tmp19;
            ge_emb = { FStarC_Custard_Syntax.ns = (nm).FStarC_Custard_Syntax.ns;
              id = (Prims.strcat "e_" (nm).FStarC_Custard_Syntax.id);
              spec = (nm).FStarC_Custard_Syntax.spec };
            ge_knot = { FStarC_Custard_Syntax.ns = (nm).FStarC_Custard_Syntax.ns;
              id = (Prims.strcat "__knot_e_" (nm).FStarC_Custard_Syntax.id);
              spec = (nm).FStarC_Custard_Syntax.spec } } in
        let tmp20 = (!(generated)) in
        let tmp21 = (e :: tmp20) in
        ((generated) := tmp21);
        e)) group) in
      let saved = (!(building)) in
      ((building) := (FStarC_List.op_At group saved));
      (FStarC_List.iter (generate_one st) entries);
      ((building) := saved))
    | tmp4 -> (raise (NoEmbedding ((Prims.strcat "no inductive declaration for " (FStarC_Ident.string_of_lid l)))))
  ))))

and embedding_for (st : FStarC_Custard_Extract.state) (k : kind) (env : ((FStarC_Syntax_Syntax.bv * FStarC_Syntax_Syntax.bv)) list) (t : (FStarC_Syntax_Syntax.term') FStarC_Syntax_Syntax.syntax) : (FStarC_Syntax_Syntax.term') FStarC_Syntax_Syntax.syntax =
  (let t1 = (whnf st t) in
  (match (t1).FStarC_Syntax_Syntax.n with
    | (FStarC_Syntax_Syntax.Tm_refine ({ FStarC_Syntax_Syntax.b = x; phi = tmp; _ })) -> (embedding_for st k env (x).FStarC_Syntax_Syntax.sort)
    | (FStarC_Syntax_Syntax.Tm_ascribed ({ FStarC_Syntax_Syntax.tm = t2; asc = u__1; asc1 = u__2; asc2 = u__3; eff_opt = tmp; _ })) -> (embedding_for st k env t2)
    | (FStarC_Syntax_Syntax.Tm_name (bv)) when (let tmp = (find_tv env bv) in
    (match tmp with
      | (Some (v)) -> true
      | tmp1 -> false
    )) -> (let tmp = (find_tv env bv) in
      let v = (match tmp with
          | (Some (v)) -> v
        ) in
      let comb = (match k with
          | SyntaxTerm -> (lid "FStarC.Syntax.Embeddings.mk_any_emb")
          | NBETerm -> (lid "FStarC.TypeChecker.NBETerm.mk_any_emb")
        ) in
      let tmp1 = (FStarC_Syntax_Syntax.bv_to_name v) in
      let tmp2 = (tmp1 :: []) in
      (call comb [] tmp2))
    | (FStarC_Syntax_Syntax.Tm_arrow (tmp)) when (let tmp1 = (FStarC_Syntax_Util.arrow_one_ln t1) in
    (match tmp1 with
      | (Some ((tmp2, c))) -> (FStarC_Syntax_Util.is_pure_comp c)
      | None -> false
    )) -> (let tmp1 = (FStarC_Syntax_Util.arrow_one t1) in
      let tmp2 = (match tmp1 with
          | (Some (v)) -> v
        ) in
      (match tmp2 with
        | (b, c) -> (let t0 = ((b).FStarC_Syntax_Syntax.binder_bv).FStarC_Syntax_Syntax.sort in
          let t11 = (FStarC_Syntax_Util.comp_result c) in
          let comb = (match k with
              | SyntaxTerm -> (lid "FStarC.Syntax.Embeddings.e_arrow")
              | NBETerm -> (lid "FStarC.TypeChecker.NBETerm.e_arrow")
            ) in
          let tmp3 = (embedding_for st k env t0) in
          let tmp4 = (embedding_for st k env t11) in
          let tmp5 = (tmp4 :: []) in
          let tmp6 = (tmp3 :: tmp5) in
          (call comb (t0 :: (t11 :: [])) tmp6))
      ))
    | (FStarC_Syntax_Syntax.Tm_app (tmp)) -> (let tmp1 = (FStarC_Syntax_Util.head_and_args_full t1) in
      (match tmp1 with
        | (head, args) -> (let tys = (FStarC_List.map Custard_FStar_Pervasives_Native.fStar_Pervasives_Native_fst args) in
          let tmp2 = (embedding_head st k head) in
          (match tmp2 with
            | (Some (f)) -> (let tmp3 = (FStarC_List.map (embedding_for st k env) tys) in
              (call f tys tmp3))
            | None -> (let tmp3 = (FStarC_Syntax_Print.fStarC_Class_Show_show__syntax_term' t1) in
              let tmp4 = (Prims.strcat "no embedding for " tmp3) in
              let tmp5 = (NoEmbedding (tmp4)) in
              (raise tmp5))
          ))
      ))
    | (FStarC_Syntax_Syntax.Tm_fvar (fv)) -> (let tmp = (embedding_head st k t1) in
      (match tmp with
        | (Some (f)) -> (call f [] [])
        | None -> (let tmp1 = (FStarC_Custard_Extract.tcenv st) in
          let tmp2 = (FStarC_TypeChecker_Env.fv_has_attr tmp1 fv FStarC_Parser_Const.plugin_attr) in
          let tmp3 = (not tmp2) in
          (if tmp3 then (let tmp4 = (FStarC_Syntax_Print.fStarC_Class_Show_show__syntax_term' t1) in
          let tmp5 = (Prims.strcat "no embedding for " tmp4) in
          let tmp6 = (NoEmbedding (tmp5)) in
          (raise tmp6)) else (match k with
            | NBETerm -> (nbe_unsupported t1)
            | SyntaxTerm -> (let l = (FStarC_Syntax_Syntax.lid_of_fv fv) in
              (ensure_generated st l);
              let tmp4 = (find_generated l) in
              (match tmp4 with
                | (Some (g)) -> (FStarC_Syntax_Syntax.bv_to_name (g).ge_ph)
                | None -> (let tmp5 = (FStarC_Syntax_Print.fStarC_Class_Show_show__syntax_term' t1) in
                  let tmp6 = (Prims.strcat "no embedding for " tmp5) in
                  let tmp7 = (NoEmbedding (tmp6)) in
                  (raise tmp7))
              ))
          )))
      ))
    | tmp -> (let tmp1 = (FStarC_Syntax_Print.fStarC_Class_Show_show__syntax_term' t1) in
      let tmp2 = (Prims.strcat "cannot embed type " tmp1) in
      let tmp3 = (NoEmbedding (tmp2)) in
      (raise tmp3))
  ))

let rec peel_type_args__go (rest : FStarC_Syntax_Syntax.bv) (tvs : (FStarC_Syntax_Syntax.bv) list) : (FStarC_Syntax_Syntax.pat') FStarC_Syntax_Syntax.withinfo_t =
  (match tvs with
    | [] -> (pat_of (FStarC_Syntax_Syntax.Pat_var (rest)))
    | (v :: tvs1) -> (let tmp = (lid "FStar.Pervasives.Native.Mktuple2") in
      let tmp1 = (ctor_fv tmp) in
      let tmp2 = (FStarC_Syntax_Syntax.new_bv None FStarC_Syntax_Syntax.tun) in
      let tmp3 = (FStarC_Syntax_Syntax.Pat_var (tmp2)) in
      let tmp4 = (pat_of tmp3) in
      let tmp5 = (tmp4, false) in
      let tmp6 = (tmp5 :: []) in
      let tmp7 = (((pat_of (FStarC_Syntax_Syntax.Pat_var (v))), false) :: tmp6) in
      let tmp8 = ((dot_pat, true) :: tmp7) in
      let tmp9 = ((dot_pat, true) :: tmp8) in
      let tmp10 = (tmp1, None, tmp9) in
      let tmp11 = (FStarC_Syntax_Syntax.Pat_cons ((match tmp10 with (custard_tup, _, _) -> custard_tup), (match tmp10 with (_, custard_tup, _) -> custard_tup), (match tmp10 with (_, _, custard_tup) -> custard_tup))) in
      let arg = (pat_of tmp11) in
      let tmp12 = (ctor_fv FStarC_Parser_Const.cons_lid) in
      let tmp13 = ((peel_type_args__go rest) tvs1) in
      let tmp14 = (tmp13, false) in
      let tmp15 = (tmp14 :: []) in
      let tmp16 = ((arg, false) :: tmp15) in
      let tmp17 = ((dot_pat, true) :: tmp16) in
      let tmp18 = (tmp12, None, tmp17) in
      let tmp19 = (FStarC_Syntax_Syntax.Pat_cons ((match tmp18 with (custard_tup, _, _) -> custard_tup), (match tmp18 with (_, custard_tup, _) -> custard_tup), (match tmp18 with (_, _, custard_tup) -> custard_tup))) in
      (pat_of tmp19))
  )

let peel_type_args (st : FStarC_Custard_Extract.state) (args_bv : FStarC_Syntax_Syntax.bv) (tvs : (FStarC_Syntax_Syntax.bv) list) (rest : FStarC_Syntax_Syntax.bv) (res_ty : (FStarC_Syntax_Syntax.term') FStarC_Syntax_Syntax.syntax) (body : (FStarC_Syntax_Syntax.term') FStarC_Syntax_Syntax.syntax) : (FStarC_Syntax_Syntax.term') FStarC_Syntax_Syntax.syntax =
  (let tmp = (lid "FStarC.Effect.failwith") in
  let tmp1 = (str "Custard: a plugin was applied to too few arguments") in
  let tmp2 = (tmp1 :: []) in
  let fail = (call tmp (res_ty :: []) tmp2) in
  let tmp3 = (FStarC_Syntax_Syntax.new_bv None (args_bv).FStarC_Syntax_Syntax.sort) in
  let tmp4 = (FStarC_Syntax_Syntax.Pat_var (tmp3)) in
  let tmp5 = (pat_of tmp4) in
  let tmp6 = (tmp5, None, fail) in
  let catchall = (FStarC_Syntax_Subst.close_branch tmp6) in
  let tmp7 = (FStarC_Syntax_Syntax.bv_to_name args_bv) in
  let tmp8 = ((peel_type_args__go rest) tvs) in
  let tmp9 = (tmp8, None, body) in
  let tmp10 = (FStarC_Syntax_Subst.close_branch tmp9) in
  let tmp11 = (tmp10 :: (catchall :: [])) in
  let tmp12 = ({ FStarC_Syntax_Syntax.scrutinee = tmp7;
      ret_opt = None;
      brs = tmp11;
      rc_opt = None } : FStarC_Syntax_Syntax.term'__Tm_match__payload) in
  let tmp13 = (FStarC_Syntax_Syntax.Tm_match (tmp12)) in
  (FStarC_Syntax_Syntax.mk tmp13 dummy))

let signature_of (st : FStarC_Custard_Extract.state) (f : FStarC_Ident.lident) (tys : ((FStarC_Syntax_Syntax.term') FStarC_Syntax_Syntax.syntax) list) : ((FStarC_Syntax_Syntax.binder) list * (FStarC_Syntax_Syntax.term') FStarC_Syntax_Syntax.syntax) =
  ((FStarC_Custard_Extract.ensure_lid_available st f);
  let tmp = (FStarC_Custard_Extract.tcenv st) in
  let tmp1 = (FStarC_TypeChecker_Env.try_lookup_lid tmp f) in
  (match tmp1 with
    | None -> (raise (NoEmbedding ((Prims.strcat "no declaration for " (FStarC_Ident.string_of_lid f)))))
    | (Some (((tmp2, ty), tmp3))) -> (let tmp4 = (FStarC_Syntax_Util.arrow_formals_comp ty) in
      (match tmp4 with
        | (bs, c) -> (let n = (FStarC_List.length tys) in
          (if (Prims.op_Less (FStarC_List.length bs) n) then (raise (NoEmbedding ((Prims.strcat (FStarC_Ident.string_of_lid f) " has too few binders")))) else ());
          let tmp5 = (FStarC_List.splitAt n bs) in
          (match tmp5 with
            | (imps, rest) -> ((FStarC_List.iter2 (fun b tmp6 -> (if (match (b).FStarC_Syntax_Syntax.binder_qual with
                | None -> true
                | tmp7 -> false
              ) then (raise (NoEmbedding ((Prims.strcat (FStarC_Ident.string_of_lid f) " does not begin with implicit binders")))) else ())) imps tys);
              let s = (FStarC_List.map2 (fun b t -> (FStarC_Syntax_Syntax.NT ((b).FStarC_Syntax_Syntax.binder_bv, t))) imps tys) in
              let tmp6 = (FStarC_Syntax_Subst.subst_binders s rest) in
              let tmp7 = (FStarC_Syntax_Subst.subst s (FStarC_Syntax_Util.comp_result c)) in
              (tmp6, tmp7))
          ))
      ))
  ))

let last_binders (n : Prims.int) (bs : (FStarC_Syntax_Syntax.binder) list) : (FStarC_Syntax_Syntax.binder) list =
  (let k = (FStarC_List.length bs) in
  (if (Prims.op_Less k n) then (raise (NoEmbedding ("interpretation function has too few binders"))) else ());
  (Custard_FStar_Pervasives_Native.fStar_Pervasives_Native_snd (FStarC_List.splitAt (Prims.op_Minus k n) bs)))

let fresh_bvs (bs : (FStarC_Syntax_Syntax.binder) list) : (FStarC_Syntax_Syntax.bv) list =
  (FStarC_List.map (fun b -> (FStarC_Syntax_Syntax.new_bv None ((b).FStarC_Syntax_Syntax.binder_bv).FStarC_Syntax_Syntax.sort)) bs)

let interp_term (st : FStarC_Custard_Extract.state) (k : kind) (tac : bool) (fv_lid : FStarC_Ident.lident) (n : Prims.int) (tvs : (FStarC_Syntax_Syntax.binder) list) (bs : (FStarC_Syntax_Syntax.binder) list) (res : (FStarC_Syntax_Syntax.term') FStarC_Syntax_Syntax.syntax) : (FStarC_Syntax_Syntax.term') FStarC_Syntax_Syntax.syntax =
  (let env = (FStarC_List.map (fun b -> (let tmp = (arg_typ k) in
    let tmp1 = (FStarC_Syntax_Syntax.new_bv None tmp) in
    ((b).FStarC_Syntax_Syntax.binder_bv, tmp1))) tvs) in
  let tmp = (FStarC_List.map (fun b -> ((b).FStarC_Syntax_Syntax.binder_bv).FStarC_Syntax_Syntax.sort) bs) in
  let tys = (FStarC_List.op_At tmp (res :: [])) in
  let embs = (FStarC_List.map (embedding_for st k env) tys) in
  let f = (FStarC_Syntax_Syntax.fvar fv_lid None) in
  let wrap = (fun vs res_ty mk -> (match env with
      | [] -> (mk vs)
      | tmp1 -> (let tmp2 = (FStarC_Util.prefix vs) in
        (match tmp2 with
          | (pre, args_bv) -> (let rest = (FStarC_Syntax_Syntax.new_bv None (args_bv).FStarC_Syntax_Syntax.sort) in
            let tmp3 = (FStarC_List.map Custard_FStar_Pervasives_Native.fStar_Pervasives_Native_snd env) in
            let tmp4 = (mk (FStarC_List.op_At pre (rest :: []))) in
            (peel_type_args st args_bv tmp3 rest res_ty tmp4))
        ))
    )) in
  (if tac then (let tmp1 = (FStarC_Class_Show.fStarC_Class_Show_show__int n) in
  let tmp2 = (Prims.strcat "FStarC.Tactics.InterpFuns.mk_tactic_interpretation_" tmp1) in
  let h = (lid tmp2) in
  let tmp3 = (signature_of st h tys) in
  (match tmp3 with
    | (hbs, hres) -> (let tmp4 = (last_binders (Prims.parse_int "4") hbs) in
      let vs = (fresh_bvs tmp4) in
      let body = (wrap vs hres (fun vs1 -> (let tmp5 = (str (Prims.strcat (FStarC_Ident.string_of_lid fv_lid) " (plugin)")) in
        let tmp6 = (tmp5 :: (f :: [])) in
        let tmp7 = (FStarC_List.map FStarC_Syntax_Syntax.bv_to_name vs1) in
        let tmp8 = (FStarC_List.op_At embs tmp7) in
        let tmp9 = (FStarC_List.op_At tmp6 tmp8) in
        (call h tys tmp9)))) in
      let tmp5 = (FStarC_List.map FStarC_Syntax_Syntax.mk_binder vs) in
      (FStarC_Syntax_Util.abs tmp5 body None))
  )) else (let h = (match k with
      | SyntaxTerm -> (let tmp1 = (FStarC_Class_Show.fStarC_Class_Show_show__int n) in
        let tmp2 = (Prims.strcat "FStarC.Syntax.Embeddings.arrow_as_prim_step_" tmp1) in
        (lid tmp2))
      | NBETerm -> (let tmp1 = (FStarC_Class_Show.fStarC_Class_Show_show__int n) in
        let tmp2 = (Prims.strcat "FStarC.TypeChecker.NBETerm.arrow_as_prim_step_" tmp1) in
        (lid tmp2))
    ) in
  let tmp1 = (signature_of st h tys) in
  (match tmp1 with
    | (hbs, hres) -> (let tmp2 = (last_binders (Prims.parse_int "3") hbs) in
      let vs = (fresh_bvs tmp2) in
      let tmp3 = (lid "FStarC.Ident.lid_of_str") in
      let tmp4 = (str (FStarC_Ident.string_of_lid fv_lid)) in
      let tmp5 = (tmp4 :: []) in
      let lid_of_str = (call tmp3 [] tmp5) in
      let body = (wrap vs hres (fun vs1 -> (let tmp6 = (FStarC_List.map FStarC_Syntax_Syntax.bv_to_name vs1) in
        let tmp7 = (FStarC_List.op_At (f :: (lid_of_str :: [])) tmp6) in
        let tmp8 = (FStarC_List.op_At embs tmp7) in
        (call h tys tmp8)))) in
      let vs1 = (match k with
          | NBETerm -> vs
          | SyntaxTerm -> (let tmp6 = (lid "FStarC.TypeChecker.Primops.Base.psc") in
            let tmp7 = (FStarC_Syntax_Syntax.fvar tmp6 None) in
            let tmp8 = (FStarC_Syntax_Syntax.new_bv None tmp7) in
            (tmp8 :: vs))
        ) in
      let tmp6 = (FStarC_List.map FStarC_Syntax_Syntax.mk_binder vs1) in
      (FStarC_Syntax_Util.abs tmp6 body None))
  ))))

let native_lid (s : string) : FStarC_Custard_Syntax.name =
  { FStarC_Custard_Syntax.ns = ("FStarC" :: ("Tactics" :: ("Native" :: [])));
    id = s;
    spec = None }

let register_fn (st : FStarC_Custard_Extract.state) (which : string) (ty : FStarC_Custard_Syntax.cty) : FStarC_Custard_Syntax.name =
  (let nm = (native_lid which) in
  (FStarC_Custard_Extract.emit st (Prims.strcat "regemb-extern:" which) (FStarC_Custard_Syntax.DExternal ({ FStarC_Custard_Syntax.dx_name = nm;
    dx_typars = [];
    dx_ty = ty;
    dx_target = None;
    dx_header = None;
    dx_flags = [] })));
  nm)

let rec ir_call__res (t : FStarC_Custard_Syntax.cty) (n : Prims.int) : FStarC_Custard_Syntax.cty =
  (if (Prims.op_Less_Equals n (Prims.parse_int "0")) then t else (match t with
    | (FStarC_Custard_Syntax.TArrow (tmp, tmp1, t1)) -> (ir_call__res t1 (Prims.op_Minus n (Prims.parse_int "1")))
    | tmp -> FStarC_Custard_Syntax.TAny
  ))

let ir_call (f : FStarC_Custard_Syntax.name) (fty : FStarC_Custard_Syntax.cty) (args : (FStarC_Custard_Syntax.expr) list) : FStarC_Custard_Syntax.expr =
  (let tmp = (ir_call__res fty (FStarC_List.length args)) in
  (FStarC_Custard_Syntax.mk (FStarC_Custard_Syntax.EApp ((FStarC_Custard_Syntax.mk (FStarC_Custard_Syntax.EQual (f, [])) fty FStarC_Custard_Syntax.E_Pure), args)) tmp FStarC_Custard_Syntax.E_Impure))

let registration (st : FStarC_Custard_Extract.state) (arity_opt : (Prims.int) option) (r : FStarC_Range_Type.range) (lb : FStarC_Syntax_Syntax.letbinding) : unit =
  (let fv = (match (lb).FStarC_Syntax_Syntax.lbname with
      | (FStar_Pervasives.Inr (v)) -> v
    ) in
  let fv_lid = (FStarC_Syntax_Syntax.lid_of_fv fv) in
  let name_str = (FStarC_Ident.string_of_lid fv_lid) in
  let key = (Prims.strcat "regemb:" name_str) in
  let tmp = (FStarC_Custard_Extract.emitted st key) in
  (if tmp then () else (let tmp1 = (FStarC_Custard_Extract.tcenv st) in
  let t = (FStarC_Custard_Mono.norm_bounded tmp1 "a plugin's type" plugin_norm_steps (lb).FStarC_Syntax_Syntax.lbtyp) in
  let tmp2 = (FStarC_Syntax_Util.arrow_formals_comp t) in
  (match tmp2 with
    | (bs, c) -> (let tmp3 = (match arity_opt with
          | None -> (bs, c)
          | (Some (k)) -> (let nbs = (FStarC_List.length bs) in
            (if ((=) k nbs) then (bs, c) else (if (Prims.op_Less k nbs) then (let tmp3 = (FStarC_Util.first_N k bs) in
            (match tmp3 with
              | (bs1, rest) -> (let tmp4 = (FStarC_Syntax_Util.arrow rest c) in
                let tmp5 = (FStarC_Syntax_Syntax.mk_Total tmp4) in
                (bs1, tmp5))
            )) else (let tmp3 = (FStarC_Class_Show.fStarC_Class_Show_show__int k) in
            let tmp4 = (FStarC_Class_Show.fStarC_Class_Show_show__int_4 nbs) in
            let tmp5 = (FStarC_Format.fmt2 "expected arity at least %s; got %s" tmp3 tmp4) in
            let tmp6 = (NoEmbedding (tmp5)) in
            (raise tmp6)))))
        ) in
      (match tmp3 with
        | (bs1, c1) -> (let arity = (FStarC_List.length bs1) in
          let tmp4 = (split_type_binders bs1) in
          (match tmp4 with
            | (tvs, bs2) -> (let n = (FStarC_List.length bs2) in
              let res = (FStarC_Syntax_Util.comp_result c1) in
              let tmp5 = (FStarC_Syntax_Util.is_pure_comp c1) in
              let tmp6 = (not tmp5) in
              let tac = (if tmp6 then (FStarC_Ident.lid_equals (FStarC_Syntax_Util.comp_effect_name c1) FStarC_Parser_Const.effect_TAC_lid) else false) in
              let tmp7 = (if (not tac) then (let tmp7 = (FStarC_Syntax_Util.is_pure_comp c1) in
                (not tmp7)) else false) in
              (if tmp7 then (raise (NoEmbedding ((Prims.strcat "no plugin for effect " (FStarC_Ident.string_of_lid (FStarC_Syntax_Util.comp_effect_name c1)))))) else ());
              (if ((=) n (Prims.parse_int "0")) then (raise (NoEmbedding ("a plugin must take at least one argument"))) else ());
              (if (tac && (Prims.op_Greater n (Prims.parse_int "20"))) then (raise (NoEmbedding ("tactic plugins can take at most 20 arguments"))) else ());
              let string_ty = (FStarC_Custard_Extract.ty_of_typ st FStarC_Syntax_Syntax.t_string) in
              let int_ty = (FStarC_Custard_Extract.ty_of_typ st FStarC_Syntax_Syntax.t_int) in
              let tmp8 = (interp_term st SyntaxTerm tac fv_lid n tvs bs2 res) in
              let interp = (compile st tmp8) in
              let tmp9 = (if tac then ((interp :: []), "register_tactic", (Prims.op_Plus arity (Prims.parse_int "1"))) else (let tmp9 = (interp_term st NBETerm tac fv_lid n tvs bs2 res) in
                let nbe = (compile st tmp9) in
                ((interp :: (nbe :: [])), "register_plugin", arity))) in
              (match tmp9 with
                | (args, which, arity1) -> (let fty = (FStarC_List.fold_right (fun a t1 -> (FStarC_Custard_Syntax.TArrow ((a).FStarC_Custard_Syntax.ty, FStarC_Custard_Syntax.E_Pure, t1))) args FStarC_Custard_Syntax.TUnit) in
                  let fty1 = (FStarC_Custard_Syntax.TArrow (string_ty, FStarC_Custard_Syntax.E_Pure, (FStarC_Custard_Syntax.TArrow (int_ty, FStarC_Custard_Syntax.E_Pure, fty)))) in
                  let reg = (register_fn st which fty1) in
                  let body = (ir_call reg fty1 (FStarC_List.op_At ((FStarC_Custard_Syntax.mk (FStarC_Custard_Syntax.EConst ((FStarC_Custard_Syntax.CString (name_str)))) string_ty FStarC_Custard_Syntax.E_Pure) :: ((FStarC_Custard_Syntax.mk (FStarC_Custard_Syntax.EConst ((FStarC_Custard_Syntax.CInt (arity1, FStar_IntegerLiteral.Dec, None)))) int_ty FStarC_Custard_Syntax.E_Pure) :: [])) args)) in
                  let nm = (FStarC_Custard_Extract.name_of_lid fv_lid) in
                  (FStarC_Custard_Extract.emit st key (FStarC_Custard_Syntax.DLet ({ FStarC_Custard_Syntax.dl_name = { FStarC_Custard_Syntax.ns = (nm).FStarC_Custard_Syntax.ns;
                      id = (Prims.strcat "__plugin_" (nm).FStarC_Custard_Syntax.id);
                      spec = (nm).FStarC_Custard_Syntax.spec };
                    dl_typars = [];
                    dl_binders = [];
                    dl_ret = FStarC_Custard_Syntax.TUnit;
                    dl_eff = FStarC_Custard_Syntax.E_Impure;
                    dl_body = body;
                    dl_flags = (FStarC_Custard_Syntax.Root :: ((FStarC_Custard_Syntax.Comment ((Prims.strcat "Plugin registration for " name_str))) :: [])) }))))
              ))
          ))
      ))
  ))))

let warn_not_implemented (r : FStarC_Range_Type.range) (what : string) (msg : string) : unit =
  (let tmp = (FStarC_Errors.lookup FStarC_Errors_Codes.Warning_PluginNotImplemented) in
  let tmp1 = (FStarC_Errors.error_number tmp) in
  let tmp2 = (FStarC_Class_PP.fStarC_Class_PP_pp__int tmp1) in
  let tmp3 = (FStar_Pprint.op_Hat_Slash_Hat tmp2 (FStarC_Errors_Msg.text "to carry on.")) in
  let tmp4 = (FStar_Pprint.op_Hat_Hat (FStarC_Errors_Msg.text "Use --warn_error -") tmp3) in
  let tmp5 = (tmp4 :: []) in
  let tmp6 = ((FStar_Pprint.prefix (Prims.parse_int "2") (Prims.parse_int "1") (FStarC_Errors_Msg.text (FStarC_Format.fmt1 "Plugin `%s' can not run natively because:" what)) (FStarC_Errors_Msg.text msg)) :: tmp5) in
  (FStarC_Errors.fStarC_Errors_log_issue__range_list_document r FStarC_Errors_Codes.Warning_PluginNotImplemented tmp6))

let handle_sigelt (st : FStarC_Custard_Extract.state) (arity_opt : (Prims.int) option) (se : FStarC_Syntax_Syntax.sigelt) : unit =
  (match (se).FStarC_Syntax_Syntax.sigel with
    | (FStarC_Syntax_Syntax.Sig_let ({ FStarC_Syntax_Syntax.lbs = (tmp, lbs); lids = tmp1; _ })) -> (FStarC_List.iter (fun lb -> (let name = (fun tmp2 -> (match (lb).FStarC_Syntax_Syntax.lbname with
          | (FStar_Pervasives.Inr (fv)) -> (FStarC_Ident.string_of_lid (FStarC_Syntax_Syntax.lid_of_fv fv))
          | (FStar_Pervasives.Inl (bv)) -> (FStarC_Ident.string_of_id (bv).FStarC_Syntax_Syntax.ppname)
        )) in
      (try (registration st arity_opt (se).FStarC_Syntax_Syntax.sigrng lb) with
        | (NoEmbedding (msg)) -> (warn_not_implemented (se).FStarC_Syntax_Syntax.sigrng (name ()) msg)
        | e -> ((FStarC_Errors.fStarC_Errors_log_issue__range_list_document (se).FStarC_Syntax_Syntax.sigrng FStarC_Errors_Codes.Warning_PluginNotImplemented ((FStarC_Errors_Msg.text (FStarC_Format.fmt1 "While registering plugin `%s':" (name ()))) :: []));
          (raise e))
      ))) lbs)
    | tmp -> ()
  )

let handle_module (st : FStarC_Custard_Extract.state) (roots : (FStarC_Ident.lident) list) (md : FStarC_Syntax_Syntax.modul) : unit =
  (let tmp = (requested roots md) in
  let tmp1 = (not tmp) in
  (if tmp1 then () else (FStarC_List.iter (fun se -> (let tmp2 = (plugin_arity (se).FStarC_Syntax_Syntax.sigattrs) in
  (match tmp2 with
    | None -> ()
    | (Some (tmp3)) when (FStarC_List.existsb (fun tmp4 -> (match tmp4 with
      | (FStarC_Syntax_Syntax.Projector (u__1, u__2)) -> true
      | (FStarC_Syntax_Syntax.Discriminator (tmp5)) -> true
      | tmp5 -> false
    )) (se).FStarC_Syntax_Syntax.sigquals) -> ()
    | (Some (arity_opt)) -> (handle_sigelt st arity_opt se)
  ))) (md).FStarC_Syntax_Syntax.declarations)))

