(* Generated by F* Custard extraction. Do not edit. *)
[@@@ocaml.warning "-3-5-8-11-20-26-27-28-32-33-34-35-37-39-50-57-60-69-70"]

type goal = {
  goal_main_env : FStarC_TypeChecker_Env.env;
  goal_ctx_uvar : FStarC_Syntax_Syntax.ctx_uvar;
  opts : (FStarC_Options.option_val) FStarC_PSMap.t;
  is_guard : bool;
  label : string;
}

type guard_policy =
  | Goal
  | SMT
  | SMTSync
  | Force
  | ForceSMT
  | Drop


type proofstate = {
  main_context : FStarC_TypeChecker_Env.env;
  all_implicits : (FStarC_TypeChecker_Common.implicit) list;
  goals : (goal) list;
  smt_goals : (goal) list;
  splice_quals : (FStarC_Syntax_Syntax.qualifier) list;
  splice_attrs : ((FStarC_Syntax_Syntax.term') FStarC_Syntax_Syntax.syntax) list;
  depth : Prims.int;
  u___dump : (proofstate -> (string -> unit));
  psc : FStarC_TypeChecker_Primops_Base.psc;
  entry_range : FStarC_Range_Type.range;
  guard_policy : guard_policy;
  freshness : Prims.int;
  tac_verb_dbg : bool;
  local_state : ((FStarC_Syntax_Syntax.term') FStarC_Syntax_Syntax.syntax) FStarC_PSMap.t;
  urgency : Prims.int;
  dump_on_failure : bool;
}

type ref_proofstate = (proofstate ref)

type direction =
  | TopDown
  | BottomUp


type ctrl_flag =
  | Continue
  | Skip
  | Abort


let goal_type (g : goal) : (FStarC_Syntax_Syntax.term') FStarC_Syntax_Syntax.syntax =
  (FStarC_Syntax_Util.ctx_uvar_typ (g).goal_ctx_uvar)

let mk_goal (env : FStarC_TypeChecker_Env.env) (u : FStarC_Syntax_Syntax.ctx_uvar) (o : (FStarC_Options.option_val) FStarC_PSMap.t) (b : bool) (l : string) : goal =
  { goal_main_env = env; goal_ctx_uvar = u; opts = o; is_guard = b; label = l }

let goal_env (g : goal) : FStarC_TypeChecker_Env.env =
  (g).goal_main_env

let goal_witness (g : goal) : (FStarC_Syntax_Syntax.term') FStarC_Syntax_Syntax.syntax =
  (FStarC_Syntax_Syntax.mk (FStarC_Syntax_Syntax.Tm_uvar ((g).goal_ctx_uvar, ([], FStarC_Syntax_Syntax.NoUseRange))) FStarC_Range_Type.dummyRange)

let check_goal_solved' (goal : goal) : ((FStarC_Syntax_Syntax.term') FStarC_Syntax_Syntax.syntax) option =
  (FStarC_Syntax_Unionfind.find (((goal).goal_ctx_uvar).FStarC_Syntax_Syntax.ctx_uvar_head, ((goal).goal_ctx_uvar).FStarC_Syntax_Syntax.ctx_uvar_head1, ((goal).goal_ctx_uvar).FStarC_Syntax_Syntax.ctx_uvar_head2))

let check_goal_solved (goal : goal) : bool =
  (let tmp = (check_goal_solved' goal) in
  (match tmp with
    | (Some (v)) -> true
    | tmp1 -> false
  ))

let goal_with_env (g : goal) (env : FStarC_TypeChecker_Env.env) : goal =
  (let c = (g).goal_ctx_uvar in
  let tmp = (FStarC_TypeChecker_Env.all_binders env) in
  let c' = { FStarC_Syntax_Syntax.ctx_uvar_head = (c).FStarC_Syntax_Syntax.ctx_uvar_head;
      ctx_uvar_head1 = (c).FStarC_Syntax_Syntax.ctx_uvar_head1;
      ctx_uvar_head2 = (c).FStarC_Syntax_Syntax.ctx_uvar_head2;
      ctx_uvar_gamma = (env).FStarC_TypeChecker_Env.gamma;
      ctx_uvar_binders = tmp;
      ctx_uvar_reason = (c).FStarC_Syntax_Syntax.ctx_uvar_reason;
      ctx_uvar_range = (c).FStarC_Syntax_Syntax.ctx_uvar_range;
      ctx_uvar_meta = (c).FStarC_Syntax_Syntax.ctx_uvar_meta } in
  { goal_main_env = env;
    goal_ctx_uvar = c';
    opts = (g).opts;
    is_guard = (g).is_guard;
    label = (g).label })

let goal_of_goal_ty (env : FStarC_TypeChecker_Env.env) (typ : (FStarC_Syntax_Syntax.term') FStarC_Syntax_Syntax.syntax) : (goal * FStarC_TypeChecker_Common.guard_t) =
  (let tmp = (FStarC_TypeChecker_Env.new_implicit_var_aux "proofstate_of_goal_ty" (typ).FStarC_Syntax_Syntax.pos env typ FStarC_Syntax_Syntax.Strict None false) in
  (match tmp with
    | (u, (ctx_uvar, tmp1), g_u) -> (let tmp2 = (FStarC_Options.peek ()) in
      let g = (mk_goal env ctx_uvar tmp2 false "") in
      (g, g_u))
  ))

let fStarC_Class_Show_show__guard_policy (tmp : guard_policy) : string =
  (match tmp with
    | Goal -> "Goal"
    | SMT -> "SMT"
    | SMTSync -> "SMTSync"
    | Force -> "Force"
    | ForceSMT -> "ForceSMT"
    | Drop -> "Drop"
  )

let goal_of_implicit (env : FStarC_TypeChecker_Env.env) (i : FStarC_TypeChecker_Common.implicit) : goal =
  (let tmp = (FStarC_Options.peek ()) in
  (mk_goal { FStarC_TypeChecker_Env.solver = (env).FStarC_TypeChecker_Env.solver;
    range = (env).FStarC_TypeChecker_Env.range;
    curmodule = (env).FStarC_TypeChecker_Env.curmodule;
    gamma = ((i).FStarC_TypeChecker_Common.imp_uvar).FStarC_Syntax_Syntax.ctx_uvar_gamma;
    gamma_sig = (env).FStarC_TypeChecker_Env.gamma_sig;
    gamma_cache = (env).FStarC_TypeChecker_Env.gamma_cache;
    modules = (env).FStarC_TypeChecker_Env.modules;
    expected_typ = (env).FStarC_TypeChecker_Env.expected_typ;
    sigtab = (env).FStarC_TypeChecker_Env.sigtab;
    attrtab = (env).FStarC_TypeChecker_Env.attrtab;
    instantiate_imp = (env).FStarC_TypeChecker_Env.instantiate_imp;
    effects = (env).FStarC_TypeChecker_Env.effects;
    generalize = (env).FStarC_TypeChecker_Env.generalize;
    letrecs = (env).FStarC_TypeChecker_Env.letrecs;
    rec_names = (env).FStarC_TypeChecker_Env.rec_names;
    top_level = (env).FStarC_TypeChecker_Env.top_level;
    check_uvars = (env).FStarC_TypeChecker_Env.check_uvars;
    use_eq_strict = (env).FStarC_TypeChecker_Env.use_eq_strict;
    is_iface = (env).FStarC_TypeChecker_Env.is_iface;
    admit = (env).FStarC_TypeChecker_Env.admit;
    phase1 = (env).FStarC_TypeChecker_Env.phase1;
    failhard = (env).FStarC_TypeChecker_Env.failhard;
    flychecking = (env).FStarC_TypeChecker_Env.flychecking;
    uvar_subtyping = (env).FStarC_TypeChecker_Env.uvar_subtyping;
    intactics = (env).FStarC_TypeChecker_Env.intactics;
    nocoerce = (env).FStarC_TypeChecker_Env.nocoerce;
    tc_term = (env).FStarC_TypeChecker_Env.tc_term;
    typeof_tot_or_gtot_term = (env).FStarC_TypeChecker_Env.typeof_tot_or_gtot_term;
    universe_of = (env).FStarC_TypeChecker_Env.universe_of;
    typeof_well_typed_tot_or_gtot_term = (env).FStarC_TypeChecker_Env.typeof_well_typed_tot_or_gtot_term;
    teq_nosmt_force = (env).FStarC_TypeChecker_Env.teq_nosmt_force;
    subtype_nosmt_force = (env).FStarC_TypeChecker_Env.subtype_nosmt_force;
    qtbl_name_and_index = (env).FStarC_TypeChecker_Env.qtbl_name_and_index;
    qtbl_name_and_index1 = (env).FStarC_TypeChecker_Env.qtbl_name_and_index1;
    fv_delta_depths = (env).FStarC_TypeChecker_Env.fv_delta_depths;
    proof_ns = (env).FStarC_TypeChecker_Env.proof_ns;
    synth_hook = (env).FStarC_TypeChecker_Env.synth_hook;
    try_solve_implicits_hook = (env).FStarC_TypeChecker_Env.try_solve_implicits_hook;
    splice = (env).FStarC_TypeChecker_Env.splice;
    mpreprocess = (env).FStarC_TypeChecker_Env.mpreprocess;
    postprocess = (env).FStarC_TypeChecker_Env.postprocess;
    identifier_info = (env).FStarC_TypeChecker_Env.identifier_info;
    tc_hooks = (env).FStarC_TypeChecker_Env.tc_hooks;
    dsenv = (env).FStarC_TypeChecker_Env.dsenv;
    nbe = (env).FStarC_TypeChecker_Env.nbe;
    strict_args_tab = (env).FStarC_TypeChecker_Env.strict_args_tab;
    disc_proj_tab = (env).FStarC_TypeChecker_Env.disc_proj_tab;
    erasable_types_tab = (env).FStarC_TypeChecker_Env.erasable_types_tab;
    enable_defer_to_tac = (env).FStarC_TypeChecker_Env.enable_defer_to_tac;
    unif_allow_ref_guards = (env).FStarC_TypeChecker_Env.unif_allow_ref_guards;
    erase_erasable_args = (env).FStarC_TypeChecker_Env.erase_erasable_args;
    core_check = (env).FStarC_TypeChecker_Env.core_check;
    missing_decl = (env).FStarC_TypeChecker_Env.missing_decl;
    iface_todo = (env).FStarC_TypeChecker_Env.iface_todo;
    iface_hidden = (env).FStarC_TypeChecker_Env.iface_hidden;
    iface_lids = (env).FStarC_TypeChecker_Env.iface_lids;
    iface_val_lids = (env).FStarC_TypeChecker_Env.iface_val_lids } (i).FStarC_TypeChecker_Common.imp_uvar tmp false (i).FStarC_TypeChecker_Common.imp_reason))

let goal_of_ctx_uvar (g : goal) (ctx_u : FStarC_Syntax_Syntax.ctx_uvar) : goal =
  { goal_main_env = (g).goal_main_env;
    goal_ctx_uvar = ctx_u;
    opts = (g).opts;
    is_guard = (g).is_guard;
    label = (g).label }

let fStarC_Class_PP_pp__guard_policy (tmp : guard_policy) : FStar_Pprint.document =
  (let tmp1 = (fStarC_Class_Show_show__guard_policy tmp) in
  (FStar_Pprint.arbitrary_string tmp1))

let set_ps_psc (psc : FStarC_TypeChecker_Primops_Base.psc) (ps : proofstate) : proofstate =
  { main_context = (ps).main_context;
    all_implicits = (ps).all_implicits;
    goals = (ps).goals;
    smt_goals = (ps).smt_goals;
    splice_quals = (ps).splice_quals;
    splice_attrs = (ps).splice_attrs;
    depth = (ps).depth;
    u___dump = (ps).u___dump;
    psc = psc;
    entry_range = (ps).entry_range;
    guard_policy = (ps).guard_policy;
    freshness = (ps).freshness;
    tac_verb_dbg = (ps).tac_verb_dbg;
    local_state = (ps).local_state;
    urgency = (ps).urgency;
    dump_on_failure = (ps).dump_on_failure }

let set_label (l : string) (g : goal) : goal =
  { goal_main_env = (g).goal_main_env;
    goal_ctx_uvar = (g).goal_ctx_uvar;
    opts = (g).opts;
    is_guard = (g).is_guard;
    label = l }

let get_label (g : goal) : string =
  (g).label

let goal_range (g : goal) : FStarC_Range_Type.range =
  ((g).goal_main_env).FStarC_TypeChecker_Env.range

let goal_opts (g : goal) : (FStarC_Options.option_val) FStarC_PSMap.t =
  (g).opts

let tracepoint_with_psc (psc : FStarC_TypeChecker_Primops_Base.psc) (ps : proofstate) : bool =
  (let tmp = (FStarC_Options.tactic_trace ()) in
  let tmp1 = (if tmp then true else (let tmp1 = (FStarC_Options.tactic_trace_d ()) in
    (Prims.op_Less_Equals (ps).depth tmp1))) in
  (if tmp1 then (let ps1 = (set_ps_psc psc ps) in
  ((ps1).u___dump ps1 "TRACE")) else ());
  true)

let set_proofstate_range (ps : proofstate) (r : FStarC_Range_Type.range) : proofstate =
  { main_context = (ps).main_context;
    all_implicits = (ps).all_implicits;
    goals = (ps).goals;
    smt_goals = (ps).smt_goals;
    splice_quals = (ps).splice_quals;
    splice_attrs = (ps).splice_attrs;
    depth = (ps).depth;
    u___dump = (ps).u___dump;
    psc = (ps).psc;
    entry_range = (FStarC_Range_Type.set_def_range (ps).entry_range (FStarC_Range_Type.def_range r));
    guard_policy = (ps).guard_policy;
    freshness = (ps).freshness;
    tac_verb_dbg = (ps).tac_verb_dbg;
    local_state = (ps).local_state;
    urgency = (ps).urgency;
    dump_on_failure = (ps).dump_on_failure }

let incr_depth (ps : proofstate) : proofstate =
  { main_context = (ps).main_context;
    all_implicits = (ps).all_implicits;
    goals = (ps).goals;
    smt_goals = (ps).smt_goals;
    splice_quals = (ps).splice_quals;
    splice_attrs = (ps).splice_attrs;
    depth = (Prims.op_Plus (ps).depth (Prims.parse_int "1"));
    u___dump = (ps).u___dump;
    psc = (ps).psc;
    entry_range = (ps).entry_range;
    guard_policy = (ps).guard_policy;
    freshness = (ps).freshness;
    tac_verb_dbg = (ps).tac_verb_dbg;
    local_state = (ps).local_state;
    urgency = (ps).urgency;
    dump_on_failure = (ps).dump_on_failure }

let decr_depth (ps : proofstate) : proofstate =
  { main_context = (ps).main_context;
    all_implicits = (ps).all_implicits;
    goals = (ps).goals;
    smt_goals = (ps).smt_goals;
    splice_quals = (ps).splice_quals;
    splice_attrs = (ps).splice_attrs;
    depth = (Prims.op_Minus (ps).depth (Prims.parse_int "1"));
    u___dump = (ps).u___dump;
    psc = (ps).psc;
    entry_range = (ps).entry_range;
    guard_policy = (ps).guard_policy;
    freshness = (ps).freshness;
    tac_verb_dbg = (ps).tac_verb_dbg;
    local_state = (ps).local_state;
    urgency = (ps).urgency;
    dump_on_failure = (ps).dump_on_failure }

let goals_of (ps : proofstate) : (goal) list =
  (ps).goals

let smt_goals_of (ps : proofstate) : (goal) list =
  (ps).smt_goals

let is_guard (g : goal) : bool =
  (g).is_guard

