This directory is to regenerate HACL*/EverCrypt Rust bindings from `../hacl-c/_output/` using Rust's `bindgen` tool.

The `*.rs` files are directly generated into `$PULSE_HOME/pulse2rust/tests/src/dpe_`
