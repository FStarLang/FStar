This is really part of the application library, but depends on some extracted
lib modules so it cannot be used in building the compiler. Hence, we separate it here,
so we can just include 'app' when building fstarc-bare.
